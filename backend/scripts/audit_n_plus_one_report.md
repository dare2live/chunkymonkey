# N+1 IO-in-loop Audit Report

- Total findings: 258
- HIGH: 245
- MEDIUM: 2
- LOW: 11
- P-4 baseline: 21
- Mode: WARN-only

## Top 50 Findings

| Severity | File | Line | Pattern | Suggested fix | Snippet |
|---|---|---:|---|---|---|
| HIGH | `backend/main.py` | 183 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for k, v in settings.items(): / conn.execute(` |
| HIGH | `backend/routers/institution.py` | 549 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for l2, v2 in sorted(v1["children"].items(), key=lambda x: -x[1]["stocks"]): / stat = conn.execute("""` |
| HIGH | `backend/routers/strategy_preset.py` | 58 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for name, payload in seed_presets: / conn.execute(` |
| HIGH | `backend/routers/updater.py` | 820 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table_name in existing_tables: / conn.execute(f"DELETE FROM {table_name}")` |
| HIGH | `backend/routers/updater.py` | 874 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for s in STEPS: / conn.execute("""` |
| HIGH | `backend/routers/updater.py` | 1143 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for sid in step_ids: / conn.execute(` |
| HIGH | `backend/routers/updater.py` | 1954 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for col in ("pricing_policy_id TEXT", "pricing_policy_hash TEXT"): / conn.execute(f"ALTER TABLE mart_institution_profile ADD COLUMN {col}")` |
| HIGH | `backend/routers/updater.py` | 1991 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for inst in institutions: / stats = conn.execute("""` |
| HIGH | `backend/routers/updater.py` | 2538 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for i in range(0, len(extra_codes), 200): / conn.execute(f"DELETE FROM mart_stock_trend WHERE stock_code IN ({placeholders})", batch)` |
| HIGH | `backend/routers/updater.py` | 2561 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for i in range(0, len(update_batch), 500): / conn.executemany(update_sql, update_batch[i:i + 500])` |
| HIGH | `backend/routers/updater.py` | 2574 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for i in range(0, len(new_batch), 500): / conn.executemany(insert_sql, new_batch[i:i + 500])` |
| HIGH | `backend/routers/updater.py` | 2730 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for _, level_name, code_col, name_col in level_specs: / rows = conn.execute(f"""` |
| HIGH | `backend/routers/updater.py` | 2755 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for r in rows: / conn.execute("""` |
| HIGH | `backend/routers/updater.py` | 4118 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for s in STEPS: / conn.execute("""` |
| HIGH | `backend/routers/updater.py` | 4280 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for spec in missing: / conn.execute(` |
| HIGH | `backend/routers/v3_paper.py` | 107 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for r in rows: / pic = conn.execute(` |
| HIGH | `backend/scripts/audit_end_to_end.py` | 43 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table, expected_n, date_col in EXPECTED: / n = conn.execute(f"SELECT COUNT(*) FROM {table}").fetchone()[0]` |
| HIGH | `backend/scripts/audit_end_to_end.py` | 100 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for name, sql, note in checks: / r = conn.execute(sql).fetchone()` |
| HIGH | `backend/scripts/audit_end_to_end.py` | 233 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table, col in checks: / r = conn.execute(f"SELECT MAX({col}) FROM {table}").fetchone()` |
| HIGH | `backend/scripts/audit_event_timestamp.py` | 193 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for spec in EVENT_TABLES: / n_total = conn.execute(f"SELECT COUNT(*) FROM {t}").fetchone()[0]` |
| HIGH | `backend/scripts/audit_event_timestamp.py` | 214 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for kind, col in (("primary", spec["primary_ts"]), ("secondary", spec["secondary_ts"])): / n_null = conn.execute(` |
| HIGH | `backend/scripts/audit_live_dashboard.py` | 58 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for pid in LIVE_PORTFOLIOS: / r = conn.execute(` |
| HIGH | `backend/scripts/audit_p0a_panel.py` | 171 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for h in (5, 10, 20): / bad = conn.execute(f"""` |
| HIGH | `backend/scripts/audit_p0a_panel.py` | 197 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table in ("mart_p0a_label_panel", feature_panel): / bad = conn.execute(f"""` |
| HIGH | `backend/scripts/audit_pit_integrity.py` | 209 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table in tier_tables: / row = conn.execute(` |
| HIGH | `backend/scripts/audit_pit_integrity.py` | 265 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for tables, severity, tier_label in tiers: / cols = {c[0] for c in conn.execute(f"DESCRIBE {table}").fetchall()}` |
| HIGH | `backend/scripts/audit_pit_integrity.py` | 345 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for signal_date in candidate_dates: / n_violating = conn.execute(` |
| HIGH | `backend/scripts/audit_survivorship.py` | 385 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for sig_date in SPOT_CHECK_DATES: / r[0] for r in conn.execute(` |
| HIGH | `backend/scripts/audit_tdx_data_need_coverage.py` | 139 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in DDL.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/audit_universe_coverage.py` | 141 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for d in days: / n = conn.execute(` |
| HIGH | `backend/scripts/audit_universe_coverage.py` | 420 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for d in samples: / next_rows = conn.execute(` |
| HIGH | `backend/scripts/backtest_model_portfolio.py` | 463 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for curve in curves: / conn.executemany(` |
| HIGH | `backend/scripts/backtest_walkforward_portfolio.py` | 347 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for r in rows: / conn.execute(` |
| HIGH | `backend/scripts/bootstrap_model_lifecycle.py` | 77 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for r in rows: / current = conn.execute(` |
| HIGH | `backend/scripts/build_architecture_inventory.py` | 172 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_architecture_inventory.py` | 647 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for candidate in candidates: / row = conn.execute(f"SELECT CAST(MAX({_quote_ident(actual)}) AS VARCHAR) AS latest FROM {table_ref}").fetchone()` |
| HIGH | `backend/scripts/build_architecture_inventory.py` | 934 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table in INVENTORY_TABLES: / conn.execute(f"DELETE FROM {table} WHERE run_id = ?", (run_id,))` |
| HIGH | `backend/scripts/build_candidate_feature_panel.py` | 117 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_drift_safe_feature_candidates.py` | 83 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_feature_association_duck.py` | 121 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_feature_association_duck.py` | 552 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for fold in ranges: / conn.execute("DROP TABLE IF EXISTS __feature_assoc_fold_base")` |
| HIGH | `backend/scripts/build_feature_association_duck.py` | 553 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for fold in ranges: / conn.execute(` |
| HIGH | `backend/scripts/build_feature_catalog_current.py` | 113 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_feature_drift_mitigation_panel.py` | 93 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_feature_drift_mitigation_panel.py` | 227 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for label in labels: / conn.execute(f"ALTER TABLE fact_feature_panel_candidate ADD COLUMN IF NOT EXISTS {_quote_ident(label)} REAL")` |
| HIGH | `backend/scripts/build_feature_drift_mitigation_panel.py` | 231 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for column in columns: / conn.execute(f"ALTER TABLE fact_feature_panel_candidate ADD COLUMN IF NOT EXISTS {_quote_ident(column)} REAL")` |
| HIGH | `backend/scripts/build_feature_drift_root_cause.py` | 97 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_feature_drift_root_cause.py` | 298 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for table in OUTPUT_TABLES: / conn.execute(f"DELETE FROM {table} WHERE run_id = ?", (run_id,))` |
| HIGH | `backend/scripts/build_feature_rank_matrix_duck.py` | 117 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for stmt in sql.split(";"): / conn.execute(stmt)` |
| HIGH | `backend/scripts/build_feature_rank_matrix_duck.py` | 394 | SQL_EXECUTE_IN_FOR_LOOP | Batch rows and move execute/executemany outside the loop, or replace the loop with set-based SQL. | `for row in rows[max_entries:]: / conn.execute(f"DROP TABLE IF EXISTS {_quote_ident(table_name)}")` |
