# Legacy 手工观察账本配置。NONCONFORMING: 只按 qfq close 近似记账，没有订单/成交、
# T+1、停牌、涨跌停或名义可成交价模型；不得称为 Tier4 paper execution/StrategyRelease。
version: 1
surface_status: legacy_nonconforming_observation

init_cash: 1000000          # 初始资金 100 万 (用户定)
benchmark_entity: index_daily    # 超额基准 SERVE entity
benchmark_code: "000300.SH"      # HS300 (用户拍板超额基准)

# 近似观察值：记入/结束按最新完成交易日 qfq close；费率只是研究估算，不是成交证据。
commission_rate: 0.00025    # 佣金 万2.5 双边
stamp_tax_rate: 0.001       # 印花税 千1 卖方
