# Institution-follow (Scheme 7) alpha guardrails.
#
# Rules-in-yaml (CLAUDE.md §3): thresholds for the institution alpha modules live
# here, not hardcoded in services/*.py. Edit a value here to change behaviour;
# business code only reads this file.

# northbound 块已退役 (2026-06-19): 其唯一守护对象 fact_hsgt_daily 已物删 (akshare HSGT
# 停 2024-08 + 0 live消费者 + NorthboundAlpha 消费者已删)。per-stock 北向持股披露 ~2025-07
# 停 (见 memory project-northbound-holdings-discontinued), 不再接入。本文件暂留作未来
# institution alpha guardrails 占位 (当前无 live rule)。
