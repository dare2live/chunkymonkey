# sector_membership_serve.yaml — Cap CX-3 decision-facing sector membership facet
# Thin trust wrapper over pulse membership leaf (list_sector_members).
# Authority: analysis/FOUNDATION_EXECUTION_PLAN.md §2 (CX-3 DONE)
# DC chains: observation-date fact_dc_member_daily snapshot + SLA fail-closed.
# SW chain: display snapshot (is_new='Y') — honesty note, not PIT (feature side uses v_sw_industry_pit).
version: 1
surface_version: decision_sector_membership_v0
disclaimer: "板块成分宇宙为展示/导航用 membership serve；东财链按快照日；申万链为当前成分快照（非 PIT）。非买卖指令。"
chains: [dc_industry, dc_concept, sw_industry]
# DC membership as_of lag vs latest completed trade date.
sla_max_lag_calendar_days: 1
# SW is never claimed PIT on this surface.
sw_membership_pit: false
