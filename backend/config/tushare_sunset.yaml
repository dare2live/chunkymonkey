# TuShare 授权日落决策 — 每个仍在 tushare 的域，到期后怎么办
#
# 硬事实: 授权 2026-09-10 到期，业主已决定**不续期**。tushare.py 在 expires_at <= now 时
# 直接 raise TuShareAuthorizationError，是硬停不是降级。
#
# 本文件是**决策记录 + 执法输入**，不是叙述: 每个域必须有 decision，
# 缺决策 = 到期后静默断流且无人知道它断了 —— 项目教训 [[feedback-defer-requires-detectability]]
# 「说『先不做』前必答: 它出现时哪行代码告诉我」。
#
# decision 取值:
#   replace        — 已有已验证替代，必须在到期前切换 (must_by 给期限)
#   derive         — 可由「已注册域 + 公开规则」推导，退役取数域 (根因立法 §5.9)
#   freeze         — 明确接受断流: 到期后台账不再更新，消费方读到的是最后一次同步的
#                    冻结快照。原 accept/accept_outage 两个值在此合并 (2026-09-04
#                    收口) —— 那两者的区分点「有没有活跃消费方」本应由
#                    `chunkyctl lineage impact` 机器判定，手写在这份台账里已经判错
#                    三次 (moneyflow/index_dailybasic/cyq_perf 手写 accept 但实测
#                    均有活跃消费方)。谁在读、断流后果是什么，写在 evidence 里；
#                    "有没有人用"不再是 decision 取值的一部分。
#   retire         — 域本身退役: 不再需要这份数据 (连"接受旧值冻结"都不需要，
#                    因为没有活跃消费方)
#   undecided      — 尚未裁决 (**这个值只应短暂存在**；门应把它当 FAIL 而非 WARN)
#
# evidence 必须是本项目实测出的数字或 commit，不接受"据说/应该"。evidence 是本文件
# 唯一保留的自由文本槽——2026-09-04 收口前台账里还有 verdict_deviation/note/impact/
# risk/caveat/fallback/residual_risk/impact_if_lost/done_evidence/blocked_by/
# blocked_by_resolved/frozen_at 十二个键，全部没有任何 loader 读过 (只有 checker
# 读 decision/status/replacement + 顶层 authorization_expires/undecided_domains)。
# 叙事(为什么偏离、当时怎么想)进 commit message，不进这张表；可从别处推导的事实
# (frozen_at = 顶层 authorization_expires) 不重复登记。字段必须满足二者之一才能
# 留在这张表: (a) 至少一个 loader 读它且写错/缺失时有门/测试会红 (b) 是这张表
# 唯一的自由文本槽 evidence。KNOWN_KEYS 之外的键 = FAIL, 见 check_tushare_sunset.py。
#
# 字段-决策矩阵 (谁能带哪些额外字段):
#   replace  → must_by, replacement, status, done_at 均可
#   derive   → replacement, status, done_at 可 (无 must_by: 推导链不是"换源"，无期限可催)
#   retire / freeze / undecided → 只有 decision + evidence，不带其他字段
#
# 同一个 target_table (sync_registry.yaml 里的物理表) 若被多个域共用，
# 这些域的 decision 必须相等——一张表不可能一半冻结一半换源。

version: 1
authorization_expires: "2026-09-10"
renew: false

# 未裁决域 (到期前必须做出裁决，否则静默断流)。曾经嵌在 domains 字典里和一个
# dict-of-dicts 混装 (checker 要三处特判才能把它摘出来)，2026-09-04 收口上移到
# 顶层——它本来就不是一个域条目，是一份名单。
undecided_domains: []

domains:

  # ── 已完成 ──────────────────────────────────────────────
  trade_cal:
    decision: derive
    status: done
    replacement: calendar_rule
    evidence: "交易日 = 周一~周五 − 法定节假日; 1990-2026 共 13,162 天与在库日历
      is_open/pretrade_date 逐字段零差异; 22 年『周末开市』例外合计 0 天。
      零供应商依赖，未来交易日不再受任何源限制。"

  # ── replace: 有已验证替代，到期前必须切 ──────────────────
  daily:
    decision: replace
    replacement: tdxhub
    must_by: "2026-09-10"
    evidence: "通达信全市场 5,208 只 × 9 字段与 canonical 逐项零差异 (46,872 项);
      除权日 pre_close 15/15 正确; 全历史 0.6s/票; 停牌语义与项目地基一致(不给行);
      TdxhubSource 已实现并通过真实端到端验收，adapter 就绪待启用。"
    status: done
    done_at: "2026-09-01"

  stock_basic:
    decision: replace
    replacement: fuyao
    must_by: "2026-09-10"
    evidence: "扶摇 /api/meta/tickers/list 筛 asset_type=a-share 得 5,563 只，
      TuShare 的 5,550 只 100% 被覆盖 (严格超集，多出 13 只为待上市新股)，且含 BJ 343 只。
      消费方 security_master 实际只读 symbol/name/ts_code 三列 + market 用于排北交所。"
    status: done
    done_at: "2026-09-01"

  adj_factor:
    decision: replace
    replacement: tdxhub
    must_by: "2026-09-10"
    evidence: "通达信 xdxr() 提供除权除息事件 (600000 有 88 条)，已在
      TdxhubSource._adjusted_pre_close 生产代码中用于逐日复权，机制已跑通。"

  limit_list_d:
    decision: replace
    replacement: fuyao
    must_by: "2026-09-10"
    evidence: "扶摇三池端点已注册为域 (fuyao_limit_up/down/break_pool)，
      涨停池 20200623 起 (早于 TuShare limit_list_d 的 2023-01 两年半)。"

  moneyflow_dc:
    decision: replace
    replacement: eastmoney_push2
    must_by: "2026-09-10"
    evidence: "push2his.eastmoney.com/api/qt/stock/fflow/daykline/get 与
      raw_tushare_moneyflow_dc 同日同票**12 字段位对位匹配** (600000.SH 2026-08-28，
      金额类差 <50 元 = tushare 万元两位小数取整边界，五个 rate 字段完全相同)。
      即同一份东财数据的两个出口，非两个体系。免 key。"

  # ── derive: 可推导，退役取数域 (根因立法 §5.9) ───────────
  stk_limit:
    decision: derive
    evidence: "up/down_limit = round(pre_close × (1±band), 2)，band 由板块定
      (30/68→20%, 92/43/83/87→30%, 其余→10%; 北交所向下取整到分)。
      实测复现率 涨停价 99.82% / 跌停价 99.51%。8,654,737 行本质是几十行规则的逐票逐日展开。"

  suspend_d:
    decision: derive
    evidence: "项目地基原则即『K 线有交易 = 在交易，没有 = 不在』。
      实测正向 97.86% (S 型行当日无 K 线)、反向沪深 99.9%。"

  # ── 有替代但源当前不可用 ────────────────────────────────
  stock_st:
    decision: replace
    replacement: baostock
    must_by: "2026-09-10"
    evidence: "baostock 日K接口的 isST 是**四源中唯一的独立 ST 标志位**(非名称前缀猜测)。
      实测 402 只样本 (202 ST + 200 非 ST) 与 canonical_stock_st_daily **准确率 100.00%**
      (假阳 0 / 假阴 0)。且同一接口一次调用还同时给 OHLCV/preclose/turn/pe/pb/ps/tradestatus,
      即 daily + daily_basic + stock_st + suspend_d 四域的核心需求。"
    status: done
    done_at: "2026-09-01"

  # ── freeze (原 accept，2026-09-04 与 accept_outage 合并): 四源均无等价，明确接受断流 ────────────────────
  cyq_perf:
    decision: freeze
    evidence: "筹码分布(成本分位/胜率)，扶摇/通达信/妙想/baostock 四源枚举均无等价物。"

  forecast:
    decision: freeze
    evidence: "公司自身业绩预告。妙想 forecast 模块 5 个报表全是**卖方分析师预测**，概念不同不可冒充。"

  index_dailybasic:
    decision: freeze
    evidence: "指数 pe/pb/换手。扶摇 valuations 端点文档明确排除指数；其余三源无。"

  limit_cpt_list:
    decision: freeze
    evidence: "最强板块排行。四源均无『最强板块』这一产品概念。"

  moneyflow:
    decision: freeze
    evidence: "个股买卖双向 5 档量额 (10 列)。东财两条线公开吐出的都是**单向净额型**
      (net + 5 档净额/占比)，结构不匹配；扶摇/通达信/baostock 均无。
      通达信 MAC 0x1218 是**另一口径**的失衡代理 (项目已标 accepted=False,
      unit=vendor_imbalance_unspecified, 注释 Not conserved money)，按项目法条
      『资金流单别=vendor算法=观点类，跨体系不可冒充等价』不得当替代。"

  # ── replace: 已测试替代，到期前必须切 ────────────────────
  margin_detail:
    decision: retire
    evidence: "零业务消费方: 唯一触点 fina_margin_recon.py 把它列入 BANNED_EXCHANGE_BASELINE (不可当交易所口径基准); legacy_raw_plane 标 blocked_no_publication。在库 4,820,384 行 20190102-20260826 按年连续无缺口。妙想 RPT_MARGIN_STATISTICS_STOCKS 同 grain 但从未对账, 零消费不值得投入验证。断流不影响任何在产功能; 历史留库 (删源≠删数据)。"

  moneyflow_hsgt:
    decision: retire
    evidence: "本域是全市场每日一行的聚合流量 (north_money/south_money/hgt/sgt), **与已停更的个股北向持股 hk_hold 是两码事**, 未受其影响。零消费: 唯一读取者 northbound_market_flow.py::latest_northbound_market_flow() 全仓库**零调用方**, 是死代码; legacy_raw_plane 标 sync_orphan 且在 publication_watchlist (承认'以后可能有用但现在没人用')。在库 2,776 行 20141117-20260828 连续。四源均无市场级聚合流量等价物 (妙想只有个股持股明细 RPT_MUTUAL_STOCK_HOLDRANKN_NEW, 不同产品; ifind MCP 实测两次均返空)。"

  moneyflow_ind_dc:
    decision: replace
    replacement: eastmoney_push2
    must_by: "2026-09-10"
    evidence: "**活跃消费**: market_pulse.py 五处引用驱动 mart_sector_pulse_daily 板块/概念资金龙头, 经 /api/v3/pulse 到前端 flow_board?chain=dc_industry / dc_concept / heatmap / flow_stripe, 端到端确认。在库 335,161 行 20240102-20260828 无缺口。候选 eastmoney_push2 继承姊妹域 moneyflow_dc 的证据链 (12 字段位对位匹配), 项目笔记已写明'板块/市场/北向字段体系同构', **但本域 grain 从未逐日对账**——被 push2his 限流挡住。注: 资金流供应商算法属观点类, 但 push2 是**同厂商东财同算法**换出口, 不构成跨体系冒充。fallback: 9 天内测不完则降 freeze, 后果=板块资金龙头/热力图 dc chain 冻结。"

  moneyflow_mkt_dc:
    decision: replace
    replacement: eastmoney_push2
    must_by: "2026-09-10"
    evidence: "**活跃但更薄**: market_pulse.py:802 取 mkt_net_amount 进 mart_market_pulse_daily 大盘情绪面板一列, 前端 /api/v3/pulse/sentiment 确认。属展示型消费, 无证据它 gate 任何策略信号。在库 818 行 20230417-20260828 逐年满勤。候选同 moneyflow_ind_dc (eastmoney_push2, 未测), **建议合并测试省一次限流冷却**。fallback freeze: 断流后仅该列冻结, 面板其余 9 列来自其他域不受影响。"

  report_rc:
    decision: retire
    evidence: "零业务消费方: assignment_gap_recon 只把它列为 product_mismatch 不对账; foundation_obs_serve 只是对全 43 域一视同仁的同步新鲜度监控; legacy_raw_plane 标 'historical load_analyst_reports retired; no formal plane'。**更正一条过时说法**: 所谓'中间 12 年整段挖空'指的是 2010-2019 段, 而 data_start 早已收窄到 20190102 (对齐 K 线真相源起点), 实测在库 1,684,839 行 2019-2026 **按年连续无挖空**。唯一测过的候选妙想 RPT_HSF10_RES_ORGRATING 已判 product_mismatch (研报目标价明细 ≠ 评级统计包, grain 不同)。"

  share_float:
    decision: retire
    evidence: "零业务消费方: 仅 assignment_gap_recon 对账、audit_date_bounds 的未来日期豁免样例、通用健康面板。在库 16,580,813 行 (grain 含 holder_name 故行数远大于事件数) 20190102-20260827, 2021 年 495 万是解禁潮非异常, 无挖空。候选妙想 RPTA_APP_LIFTFUTURE 已实测判定不等价: TuShare 是 holder 级明细, 妙想是 stock-day 解禁聚合, jaccard=0.3, same_product=false。"

  stk_holdernumber:
    decision: replace
    replacement: 妙想 RPT_F10_EH_HOLDERNUM
    must_by: "2026-09-10"
    evidence: "**活跃**: holdernumber_assist.py 经 DataAccess 读 v_holder_number_period, 上游 stock_dossier.py:873 进个股档案 holder_number 集中度辅助, 经 /api/v3/stock 到前端档案页, 端到端确认; 2026-07-24 owner 刚 RESTORE 过接线。在库 336,864 行 20190101-20260827 无缺口。候选已实测 (样本 600519.SH): 户数 296,404 **完全一致**、end_date 一致、ann_date=NOTICE_DATE 一致, same_product=true; 仅因**只测了 1 样本 1 截面**未切 primary。工程路径现成 (holders_aif10.py 已按同模式接 canonical)。**9 天内必做**: 扩样本覆盖 ST/北交所/密集披露日边界。fallback freeze: 档案页该字段冻结, 前端已有 fail-closed 标 holder_number_empty 不会崩。"

  stk_holdertrade:
    decision: freeze
    evidence: "**有活跃消费方但无可用替代**: institution_profile.py:72 经 compare_disclosure_research_shadow 进 Tier3 披露研究 disclosure_shadow 侧栏, 经 /api/v3/inst 到前端确认; 另有完整 E0 land→accept→canonical 管线 (canonical_stk_holdertrade_announcement 已 73,304 行)。在库 72,340 行 20190102-20260715。**两个妙想候选均已实测判定 product_mismatch**: RPT_F10_SHAREHOLDER_CHANGE (TuShare 是公告事件轴 ann_date×holder_name, 妙想是十大股东季度差分 end_date×holder_rank, 轴不同) 与 RPT_EXECUTIVE_HOLD_DETAILS (高管持股变动≠股东增减持)。断流后果: 该维度停更, 已落库 raw 72,340 + canonical 73,304 行历史保留, 回测不受影响。真要替代需重新调研产品级差异, 非简单换源。"

  stk_surv:
    decision: retire
    evidence: "零消费: 下游 mart_stock_survey_activity **已于 2026-06-28 物理删除** (data_audit_rules/data_audit 双处确认), 之后是纯 landing 表; legacy_raw_plane 标 serve_l0_declared 'no formal plane'。在库 839,098 行 20210805-20260818 无缺口。候选妙想 RPT_ORG_SURVEYNEW 已实测对账两次: surv_date 与 RECEIVE_START_DATE / NOTICE_DATE 两个候选日期字段均对不齐 (jaccard 0.34 / 0.42, same_product=false)。注: 妙想覆盖面明显更宽 (1568/751 vs 本地同期 17 条), 未来若重建该功能它是更优起点, 但轴对齐需重新设计。"

  sw_daily:
    decision: replace
    replacement: ifind_shenwan_index
    must_by: "2026-09-10"
    evidence: "**活跃且用量重**: market_pulse.py 五处 + routers/market_pulse.py:569 驱动申万 RS 双窗/rs_rank_4w/turnover_amt_share (588 码含全级指数), 前端 pulse/heatmap|rotation|drill?chain=sw_industry 三条链路端到端确认。在库 921,483 行 20190102-20260828 无缺口。候选实测: mcp__ifind-index__sector_data 查'申万一级行业银行指数 2026-08-28'返回收盘价 8.6948 + 成交额 250.1994 亿, **确认覆盖申万 L1**。**但两点未验证**: (1) L2/L3 覆盖比例未知 (588 码里大部分是 L2/L3); (2) 该 MCP 是自然语言查询接口, **不在项目已知三 adapter 之列**, 能否变成生产级 sync adapter (授权/批量/稳定接口) 未定。注: ifind=同花顺, **与代码里的'妙想'(东财 aif10 scraper) 是不同供应商**。fallback freeze: sw_industry 三条链路冻结, 但 dc_industry/dc_concept chain 不受影响, 前端仍有一条链路可看。"

  top_inst:
    decision: replace
    replacement: 妙想 RPT_OPERATEDEPT_TRADE
    must_by: "2026-09-10"
    evidence: "**活跃多消费方**: (1) market_pulse.py:739 SUM(net_buy) AS lhb_inst_net 进 mart_market_pulse_daily, 前端 pulse/sentiment; (2) 独立 B2 正式发布管线写 fact_top_inst_seat_daily, 被 check_grain_uniqueness / check_legacy_raw_plane 门禁核验。在库 1,057,771 行 20190102-20260828 无缺口。候选已实测对账 (对比对象=已发布的 fact_top_inst_seat_daily, grain=trade_date×ts_code×seat×side): **identity=true, jaccard=1.0 (526/526 完全匹配, 无截断)** —— 是精确集合对比不是抽样估计。工程范式现成 (holders_aif10.py)。9 天内建议扩对账天数确认非偶然 (当前只测 20260825 一天) 后切换。"

    status: done
    done_at: "2026-09-01"

  top_list:
    decision: replace
    replacement: 妙想 RPT_DAILYBILLBOARD_DETAILSNEW
    must_by: "2026-09-10"
    evidence: "**活跃**: market_pulse.py:734 算 lhb_count 进 mart_market_pulse_daily, 前端 pulse/sentiment 确认; 另作 top_inst 的 cross_check_domain 交叉核验落库完整性。在库 120,101 行 20190102-20260828 无缺口。候选已实测: **identity=true, jaccard=1.0 (60/60 完全匹配, 无截断)**。与 top_inst 是同一龙虎榜产品线的姊妹域, **建议一起切换**; 切换后 cross_check_domain 交叉核验要同步改成两个新源互查, 不能留一个 tushare 一个妙想各自为政。"

    status: done
    done_at: "2026-09-01"

  daily_basic:
    decision: replace
    replacement: 妙想
    must_by: "2026-09-10"
    evidence: "**活跃且是单一计算点**: segments.py (B1 分层前置件, docstring 明文'禁止各策略自己算市值段')
      只用两列 circ_mv / turnover_rate 生成 dim_stock_segment_daily; 该表再被 market_pulse / moneyflow_assist /
      pipeline/process(每日跑批入口) / rally_gt / technical_states / check_grain_uniqueness 消费,
      最终到 routers/stock_dossier + routers/market_pulse 前端。在库 8,563,435 行 2019-01-02~2026-08-28
      按年平滑 885k->1.25M 无缺口。实测茅台 2026-08-25: 换手率 0.1689% / 总市值 1.6301万亿,
      与库内**逐位精确匹配** (0.1689 vs 0.1689; 163,010,640.64万元=1.6301万亿)。
      fuyao 明确无 (assignment_gap_recon 记录'扶摇估值快照无换手/股本/市值', fuyao_dump_coverage.has_daily_basic=False);
      tdxhub 源码扫描无 turnover/circ_mv。
      **⚠ 落地风险**: 只验证 1 票 1 天 2 字段; segments 只需这 2 列所以窄口径够用, 但 MCP 是自然语言
      单次查询形态, **全市场 5000+ 票日更批量可行性未验证**, 需另案设计批量接口/限流方案。"

  index_daily_benchmark:
    decision: replace
    replacement: 妙想 index_data
    must_by: "2026-09-10"
    evidence: "**活跃**: publish_fact_index_daily/index_daily_publish -> market_pulse.py 的 HS300
      超额收益 KPI 真相源 (项目 memory 标为地基依赖), 经 market_pulse 到前端。
      在库 35,465 行 2005-01-04~2026-08-28, 覆盖 6 个基准指数 (000300.SH/000905.SH/000016.SH/
      399006.SZ/000001.SH/399001.SZ), 按年约 1450-1710 行 (6指数×~245交易日≈1470 量级吻合), 无缺口。
      实测沪深300 2026-08-25: 开盘 4542.2438 / 收盘 4552.0326 / 成交额 480,421,573.87千元=4804.2157亿,
      与库内同日**逐位精确匹配**。fuyao/tdxhub 已暴露函数均无指数行情端点。
      **局限**: 只测了 6 个基准里的 1 个, 其余 5 个 (中证500/上证50/创业板指/上证指数/深证成指)
      切换前建议补测, 但方法论已验证有效。"

  dc_member:
    decision: freeze
    evidence: "与 dc_index 完全同构的消费链 (build_dc_industry_view -> dim_stock_dc_industry ->
      routers/stock_dossier + moneyflow_assist), 另有 dc_member_publish / type_b_fact_publish_catchup /
      .moth/assertions/claims.yaml 治理断言层 / data_access.yaml / data_layers.yaml / segments.yaml 声明。
      在库 27,303,428 行 (全市场×全概念每日展开, 量级符合) 2025-01-02~2026-08-28,
      2 个 verified_low_days 已 vendor 核实为真实覆盖差异非截断。
      实测妙想查'上海板块'返回同花顺地域类 446 只成分股 —— taxonomy 体系不对 (同花顺 THS 无 PIT,
      项目红线已否决 'ths_member 出局做分类源', 妙想默认路由到 THS taxonomy = 继承同一被否决缺陷,
      不能直接用做替代)。东财 push2 直连三家指定源均未验出可用替代 (见 dc_index 条目)。"

  dc_index:
    decision: freeze
    evidence: "**活跃且直达前端**: build_dc_industry_view.py (联合 dc_member 建 dim_stock_dc_industry)
      -> routers/stock_dossier.py 前端 + services/moneyflow_assist.py。在库 272,212 行 仅 2025-01-02~2026-08-28
      (data_start 声明与实际一致, 无历史断层假象), 2 个 verified_low_days 已逐页核对 vendor 为真实低值非截断。
      fuyao dump kinds 无板块数据(has_index_dump=False); tdxhub 的 block() docstring 直接写明
      'Vendor tdx_block membership. **Not SW / DC / THS; no name crosswalk.**' 即通达信自有板块体系
      与东财(DC)不可对照。妙想 taxonomy 不对口 (查'东方财富行业板块'被路由到申万SW);
      直连 push2.eastmoney.com 探测失败: agent 侧 TLS 握手成功但返空响应, 主线**独立复现**同一结果
      (clist/get 板块列表端点返 RemoteDisconnected, 带完整 UA+Referer 亦然)。同一时段扶摇正常取回 5,563 行,
      证明**网络本身通**, 是该端点特定的反爬或会话要求。两次独立尝试均不通, 需换手段 (浏览器会话/代理)
      而非换参数。注: 姊妹域 moneyflow_dc 虽已裁决 replace->eastmoney_push2, 但全仓库**没有任何实际的
      push2 HTTP 调用代码** —— 那个裁决同样只是裁决、尚未实现, 不能当作'这条路已验证可行'的先例。"

  margin:
    decision: freeze
    evidence: "**重要更正: formal_boundaries 标的 runtime_state=retired_readonly 是过期文档标签**。
      实测 2026-09-01: legacy 镜像 raw_tushare_margin 确实停在 20260716, 但正式 land->accept 通道
      仍在活跃调 tushare —— canonical_margin_exchange_daily 最新 built_at=2026-08-28 (3天前),
      max(trade_date)=2026-08-27, margin_acceptance.py:83 source='tushare' 硬编码, 走 2026-07-23
      解冻的 on_demand 有界追赶策略。**活跃消费方**: market_pulse_scope.py -> margin_pulse_promote.yaml 门
      -> routers/market_pulse.py 前端两融/rzrqye 市场情绪面板。在库 4,533 行 (SSE+SZSE) 2019-01-02~2026-08-27 无缺口。
      **三源均无交易所级两融汇总**: fuyao/tdxhub grep margin|rzye|rzrq|融资|融券 全零命中;
      妙想只能经指数实体代理查 (000001.SH 融资余额 1.3318万亿 vs 库内 SSE 实际 1.4308万亿 **偏差7%**;
      399001.SZ 9274.59亿 vs 库内 SZSE 1.3982万亿 **偏差33%**) —— 那是**指数成分股范围**的两融,
      不是交易所全量口径, 与本域 population(sse_szse_venue_reported_margin) 不同产品。
      断流后果: 系统已有优雅降级路径 (margin_pulse_promote/market_pulse_scope 已实现 typed EMPTY
      处理 not_expected/confirmed_empty), 面板 rzrqye 停更不会崩。4,533 行历史保留供回测。
      **⚠ 需 owner 主动确认**: 它眼下**还在活跃取数**不是自然停止, 09-10 到期后若没人主动关掉这条
      on_demand 通道, 会从'正常运行'直接变成'报错'而非优雅停止 —— 建议到期前显式切成 freeze 状态。"

  income:
    decision: replace
    replacement: 妙想 GINCOME
    must_by: "2026-09-10"
    evidence: "**有真实生产消费方**: org_holding_announcement.py 用 income.f_ann_date 做机构持仓回测的
      PIT 锚点 ('Backtests join the same company first income.f_ann_date, else holders notice_date'),
      口径窄(只要每家公司每期最早的 f_ann_date)但是真实依赖。在库 286,068 行 end_date 2008-03-31~2026-06-30
      按年平滑 6,968->24,686 无缺口。实测茅台 2026Q1: 营收 547.03亿/净利 272.43亿/公告日 20260425,
      与库内**逐位精确匹配**。但项目 2026-08-27 实测 (000001.SZ, 146 期, 2008-2023):
      总营收/净利润 142-143/146 (97-98%) 很好; **公告日期只有 130/146 (89%) 匹配, 16 期里妙想的
      NOTICE_DATE 系统性比 tushare 的 f_ann_date 晚了接近整整一年** (如 f_ann_date=20080424 对应
      NOTICE_DATE=20090424)。该异常在 2026 年新鲜样本未重现, 疑似妙想对 2008-2010 早年财报用修订版
      公告日覆盖了当年首发公告日, 或对账脚本 join 逻辑对老数据配错期。
      **⚠ 切换前必须先解决**: org_holding_announcement 恰恰靠 f_ann_date 做 PIT 锚, 该字段历史段有
      系统性偏差且原因未查清, 直接切换有**真实的未来函数泄漏/回测失真风险**。当前 2026 数据看着没问题,
      2008-2010 附近老数据要先重新核实。"

  balancesheet:
    decision: replace
    replacement: 妙想 GBALANCE
    must_by: "2026-09-10"
    evidence: "三张财务表里验证最扎实的一张。项目自己 2026-08-27 实测 (fina_margin_recon.json,
      茅台 600519.SH 25 期样本, GBALANCE vs raw): total_assets 24/25, total_liab 24/25, contract_liab 25/25,
      **公告日期 f_ann_date vs NOTICE_DATE 25/25 = 100% 精确匹配** (PIT 锚点无偏)。
      消费方: 仅 fina_margin_recon.py, 其 docstring 自标 'landing orphans (no accepted publication)'
      = 零生产 feature/serve/frontend 消费方。在库 129,055 行 end_date 2016-12-31~2026-06-30,
      2016-2019 极稀疏(58/22/22/85 行)已核实是新股招股书历史孤例, 2020 起 16,746->20,782/年稳定, 无中段挖空。
      fuyao/tdxhub 均无财务报表 (2026-06-26 commit 载明'通达信全删单元4财务链迁tushare')。"

  block_trade:
    decision: replace
    replacement: 妙想 RPT_DATA_BLOCKTRADE
    must_by: "2026-09-10"
    evidence: "本批证据最硬的一个 —— 不是推测, 是项目自己 2026-08-28 (3天前) 跑过的实测:
      assignment_gap_recon.json 记录 block_trade vs RPT_DATA_BLOCKTRADE, grain=trade_date×ts_code×buyer×seller,
      **jaccard=1.0, identity=true, 82/82 行精确匹配** (妙想原始 112 行经同口径过滤后与本地一致)。
      消费方: recon 脚本(QA) + serve_read_views.yaml 注册了 v_block_trade_stock_day 但全仓库零 .py 实读
      (声明了未接线的服务面)。在库 109,875 行 2023-01-03~2026-08-27, 5 个骤降日已 vendor 核实非采集缺陷。
      fuyao/tdxhub 均无大宗交易端点。"

  fina_indicator:
    decision: replace
    replacement: 妙想(分字段裁, 非整域切)
    must_by: "2026-09-10"
    evidence: "消费方: 仅 fina_margin_recon.py, 同 balancesheet 一样是 'DataAccess L0 on raw,
      still not accepted publication' = 零生产消费方。在库 78,611 行 end_date 2019-12-31~2026-06-30
      (2019-2021 稀疏 152/353/427 行=新股历史孤例已 review), 2022 起 6,053->20,843/年稳定。
      项目 2026-08-27 实测 (000001.SZ, 28 期): **公告日期 28/28=100%匹配**; debt_to_assets 27/28;
      or_yoy 18/28; **但 roe 只有 6/28 匹配**, 其余系统性偏离 10-20% —— 大概率是 ROE 口径差异
      (加权平均 vs 期末值、摊薄 vs 基本等标准变体多), 不是数据错误; grossprofit_margin 14/28 两边同时为空。
      **裁决要点**: PIT 日期与部分衍生指标(负债率/营收增速)可直接用; **ROE 这个最常用因子不能盲切** ——
      要么在妙想侧显式指定匹配 tushare 口径的 ROE 变体, 要么改用已验证良好的 balancesheet+income 本地重算。
      167 项衍生指标只核了 4 个, 全量切换前建议补测更多字段。"

  dividend:
    decision: freeze
    evidence: "零生产消费方: 仅 fuyao_kline_recon.py + assignment_gap_recon.py, 两者 docstring 都明写
      'Read-only... Does not change kline_daily.primary or write accepted partitions' = 纯 QA 对账不进生产链路;
      grep backend/routers 与 frontend 零命中。真正生产用的复权因子来自已独立裁决的 adj_factor(replace->tdxhub),
      不依赖 dividend 表本身。在库 51,090 行 2005-03-21~2026-08-27 按年 788->4,680 平滑增长
      (event_sparse 语义下稀疏是预期, registry 已声明)。断流只损失'对新除权事件做 vs tushare 自身记录的
      复核能力', 不影响实际复权。
      **⚠ 置信度低于其他条**: 未实测妙想/fuyao 对 dividend 的替代 (时间预算优先给了有生产消费方的域);
      fuyao 的 adjustment-factors dump kind 结构上更像公司行为事件流(与 div_proc/ex_date 同构)但只被标记为
      adj_factor 的 product_mismatch, 没专门对 dividend 测过。若 owner 想保留对账能力, 值得补测。"

  index_member_all:
    decision: freeze
    evidence: "**PIT 敏感、直达前端与 GT 标注的地基级参照表**: build_sw_industry_view.py -> v_sw_industry
      -> routers/market_pulse.py 前端 + services/institution_profile.py + services/market_pulse.py +
      services/segments.py + scripts/ensure_serve_read_views.py; 另有 **rally_gt.py 直接用
      '申万 L1/L2 as-of raw_tushare_index_member_all, in_date<=底<=out_date' 做 Rally Ground Truth 分层标注**(考古 §3.4)。
      在库 7,911 行 (参照维度表非日频面板, 量级符合) in_date 1984-05-09~2026-08-14, 历年持续新增
      (2025年143行/2026年176行), 未见异常多年真空。fuyao/tdxhub 均无 (tdxhub block() 已明文声明不可与 SW 对照)。
      妙想 sector_data **确认支持历史 as-of 查询** (同一板块 2020-01-01 时点返回值区别于'最新'值, 证明有真实历史能力),
      但量级对不上(是 L1 vs L2 层级差, 非版本差, 2026-09-02 实测已排除版本不一致假设)。
      **2026-09-02 追加实测 —— 本表在分类学意义上不是 PIT 的, 且是『不一致地不 PIT』**:
      个股在既有行业间迁移**确实**留带日期的记录 (1,649/5,897 只股票有多行, is_new='N' 共 2,009 行);
      但 2021 版**整行业拆分是无痕改标** —— 中国神华 601088.SH 全表**只有一行**:
      煤炭(801950.SI), in_date=20071008, out_date=NULL, 而 801950 这个一级行业 2021 年才存在
      (2014 版下属『采掘』801020, 本表 0 行); 中国石油 601857.SH 同型。2021 版新增的 4 个 L1 里
      in_date<2021 的共 **187 行** (煤炭36/石油石化41/环保92/美容护理18), 每行都在声称某股在一个
      当时不存在的行业里待了多年。**两种语义混在同一张表且无字段可区分。**
      落到下游 (实测 feature_store.fact_rally_strata): bottom_date < 2021-07-01 的 3,446 条样本中
      **132 条 (3.83%)** 的 sw_l1 是底部当日尚不存在的行业。定性: 不是价格/收益泄漏, 是
      **分层变量里的事后视角**; 描述性分层可控, 但 sw_l1/sw_l2 一旦被当**特征**或负样本配对键用即成泄漏。
      细节与边界写在 backend/services/rally_gt.py 的模块 docstring (同日更正了原写的『= 真 PIT』)。
      **对本裁决的影响**: 换源不改变这条 (申万官方发布的也是当前版分类, 任何供应商同病),
      因此它**不构成换源阻塞**; 但也意味着替代源**不需要**具备历史 as-of 分类能力, 门槛降低。"

  index_member_all_hist:
    decision: freeze
    evidence: "与 index_member_all 同表同源 (同一张 raw_tushare_index_member_all, 按 is_new=Y/N MERGE),
      同一消费链、同一冻结影响。见 index_member_all 条目。两域共用一张物理表, 合并裁决。"
