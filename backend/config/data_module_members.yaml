# Legacy enforcement roster (NONCONFORMING target architecture) — 当前 SERVE 裸读门仍消费，
# Phase1 由每个模块的 contract manifest/typed output 边界取代后删除。这里的目录、文件和前缀
# 是过渡期 code-topology whitelist，不是新架构的业务配置或模块真相源。
#
# 数据模块成员 roster — 不变量4 (单概念单真相源 / SERVE 单一读路) 执法 (2026-06-23 边界法决议2)
#
# 成员 = 数据模块的子模块本身 (采集/清洗/加工/serving/审计/infra/真相源), 允许连数据源/读 raw —
#   它们就是数据模块, 不是"绕过的外人" (解 SERVE 建在 adapter 之上的循环依赖)。
# 非成员 (策略/信号/展示/回测消费者) 内联读 raw L0 = 违规 → 必须走 services.data_access.DataAccess.get()。
# owner=本文件 (landing→canonical→serve→consumer)。
# 用法: check_serve_read_layer.py D1 门读本 roster, 全量扫 backend/services+scripts 非成员内联 raw 读
#   (2026-07-08 前经 --bypass-scan 单独参数触发，现为默认硬门；owner=live gate/tests)。
version: 1

# 目录级成员 (整包是数据模块)
member_dirs:
  - backend/services/data_access/      # SERVE 读层 (清洗单一执行点)
  - backend/services/data_sources/     # 采集 sync_runner (acquire)
  - backend/services/pipeline/         # daily_update 四阶段编排 (获取/清洗/加工/存储) — 数据模块自己的更新引擎, 非外部消费者 (2026-06-23 重设计)
  - backend/services/technical_states/ # 纯函数派生 (读 SERVE 传入序列, 不直连)
  - backend/services/perf/             # 性能/并行基础设施 (shard/reducer, 导快照非消费raw)
  - backend/services/lineage/          # M5 血缘中枢 (字典+总指挥; 读 information_schema 元数据描述单一路径架构, 不变量4 自描述副产物, 非消费 PIT 数据)

# scripts 文件名前缀级成员 (basename 前缀)
member_script_prefixes:
  - build_        # 加工 builder (物化 L1/L2, bulk 读 clean canonical / 经 data_loaders)
  - audit_        # 审计工具 (直查真相源, 不能经被审对象读层)
  - check_        # 门脚本自身
  - repair_       # accepted/pointer/partition repair (audit writers; not SERVE consumers)
  - db_           # DB 生命周期 (compact/delete/partition)
  - ingest_       # 采集落库
  - migrate_      # 迁移工具
  - plan_         # retention 计划
  - cleanup_      # 清理工具
  - profile_      # 字段画像
  - measure_      # 一次性度量 (评估后入 sandbox)

# 具体 service 文件成员 (infra/写侧/真相源/审计)
member_service_files:
  - duck_adapter.py          # DB adapter (SERVE 建其上)
  - duckdb_compact.py        # ATTACH-copy compact after DROP/UPDATE; production alias via manifest
  - db_connection.py
  - database_manifest.py     # 库路由
  - market_db.py             # 写侧
  - market_schema.py         # DDL
  - market_read.py           # K线读 helper (serving 内部)
  # etf_db.py 已删 2026-06-29 (批3d: ETF 子系统整体退役物删)
  - holdout_guard.py         # 过渡期训练窗口边界门；不声称实现 Tier3 single-touch/release runtime
  - security_master.py       # 身份/dim 真相源
  - universe.py              # 交易日历级硬真相源 (不能依赖派生读层)
  - kline_source.py          # K线写侧 normalize
  - data_audit.py            # 审计
  - project_status.py        # L2 现查投影 (goal.md P2.2): 直查 accepted_partition /
                             #   mart_data_source_watermark / dim_trading_calendar 这些**真相源本身**
                             #   报前沿与滞后。与 data_audit 同性质 —— 审计/状态投影不能经被审
                             #   对象的读层拿数, 否则读层坏了它就跟着一起瞎。它不消费 PIT 业务
                             #   数据、不产表、不做裁决 (裁决仍归 continuity/SLA/cutover 各自的门)。
  - institution_profile.py   # M3 加工 builder (2026-07-02 W1: 读 raw/L1 物化 feature_store L2 画像;
                             #   与 build_price_kline_qfq_tushare 同性质)。**2026-07-08 决议**: 产物表
                             #   (mart_inst_profile/mart_inst_profile_dim/fact_inst_episode) 不注册进
                             #   DataAccess entity — 三表按 holder(机构名)为键非 stock code, 且
                             #   mart_inst_profile(_dim): daily process delta-gates rebuild_all()
                             #   when holders notice frontier advances (closed-loop 20260723); 无 PIT asof
                             #   列, 不符合 GenericDriver 的 "code IN(...) AND asof<=t" 单表时间序列
                             #   模型, 硬塞需扩展 DataAccess 本体(违反§4.1"零摩擦"红线)且当前 0 真实
                             #   消费方(Phase A 未开工, speculative feature 不做)。**正解**: 消费方(未来
                             #   Phase A 档案 API)走本模块既有读侧 API(list_profiles/get_profile/
                             #   recent_signals, institution_profile.py:323起, 模块自身 owns 这些表的
                             #   serving 读路, 与 builder 角色一致), 不经 DataAccess.get()。
                             #   现行边界由模块 typed API 与 check_serve_read_layer.py 共同守护。
  - continuity_guard.py      # 消费侧连续性硬门 (2026-07-03 用户定调交易日历级强制: 中间缺口喂进研究=raise; 消费方=rally_gt 第三道门+D2 builder 必接; 采集侧告警=check_continuity_integrity)
  - holders_landing_retention.py  # F3 2026-07-23: archive non-latest ACCEPTED holders landing + DELETE; cold parquet fuse; not a SERVE consumer
  - calendar_builder.py      # M2 dim 刷新 builder (2026-07-03 R1 根因3: raw_tushare_trade_cal→dim_trading_calendar 增量, 挂 acquire Step 2.96; 根治日历孤儿断链)
  - segments.py              # M3 加工 builder (2026-07-02 B1: 读 raw daily_basic+PIT行业 物化分层标签, 单一计算点)
  - rally_gt.py              # D1 GT v2 加工 builder (2026-07-02: 读 market K线 + raw stock_st/stock_basic/index_member_all,
                             #   物化 feature_store fact_rally_ground_truth/negative/strata [Type B edge 隔离];
                             #   与 institution_profile/segments 同性质; 消费方 (D2 标注) 读其产物非 raw)
  - technical_states/        # M3 加工 builder (2026-07-02 B2 正交轴形态识别; upstream=price_kline_qfq+daily/stk_limit原始价触板+index_daily+dim_trading_calendar+dim_stock_segment_daily[E轴]; consumer=D1 GT/D2 L0/C前端档案/B4 stage分布)
  - market_pulse.py          # M3 加工 builder (2026-07-02 B4+v2: follow-the-money 板块/全市场脉搏, 两链隔离; upstream=moneyflow_ind_dc/dc_index/sw_daily/index_daily/limit_list_d/daily/moneyflow_mkt_dc/index_member_all/dim_stock_segment_daily +v2: dc_member/moneyflow_dc/margin/index_dailybasic/top_list/top_inst/limit_cpt_list; consumer=C4 pulse API+D2 板块上下文; v3 2026-07-03 upstream 增 raw_tushare_moneyflow/v_sw_industry_pit/raw_tushare_daily_basic [sw链L1-L3资金流聚合] + fact_stock_form_daily [drill叶子形态]; F4 2026-07-23 rzrqye←accepted canonical SSE+SZSE via margin_pulse_promote.yaml)
  - market_pulse_serve_read.py  # S6 serve-read: drill/members/margin L0 leaf via DataAccess registry+resolver; router 禁内联 raw
  - margin_pulse_promote_gate.py  # F4 2026-07-23: promote gate + load margin_pulse_promote.yaml; serve→accepted SSE+SZSE; READY as external_aggregate only when criteria pass
  - stock_limit_publish.py   # B2 2026-07-21: fact_stock_limit_daily stock-day limit publication (raw_tushare_limit_list_d → smartmoney; serve/pulse leaf)
  - stock_moneyflow_publish.py  # B2 2026-07-21: fact_stock_moneyflow(_dc)_daily stock-day flow publication (raw moneyflow/moneyflow_dc → smartmoney; serve/pulse leaf)
  - northbound_market_flow.py  # market-day overlay of moneyflow_hsgt; not a named holder; typed API not stock-asof DataAccess
  - index_daily_publish.py      # B2 2026-07-21: fact_index_daily index×trade_date close publication (raw index_daily → smartmoney; paper/RS/tech/inst)
  - top_inst_seat_publish.py    # B2 2026-07-21: fact_top_inst_seat_daily LHB seat grain (raw top_inst → smartmoney; pulse/inst)
  - dc_member_publish.py        # B1 2026-07-21: fact_dc_member_daily observation-date DC membership PIT (raw dc_member → smartmoney; pulse/serve)
  - audit.py                 # 审计
  - data_health_snapshot.py
  - data_layer_audit.py
  # leakage_probe.py 已删 2026-06-28 (残留清理批1c: 特征面板 serving 退役)
  - update_watermark_sla.py  # 水位 SLA (infra)
  - data_loaders.py          # 取数底座 (已迁 SERVE; 本身是数据模块成员)
  # 采集层 source clients — sync 路读自身 raw watermark/count = acquire 行为, 非消费者绕过
  # (vendor 迁移[akshare→tushare/aif10]属 task#56 正交, 不改"它们是采集成员"的事实)
  # lhb_client.py 已退役物删 2026-06-29 (批2b: LHB 切 tushare top_list/top_inst, raw_lhb_daily 物删)
  - org_holding_db.py        # org_holding.duckdb routing (write-lock split from smartmoney)
  - org_holding_aif10.py     # 机构持仓采集 (aif10 sanctioned 源 §4.3; 读 raw_org_holding_aif10 期次存在)
  - org_holding_population.py  # org 存在性/人口探测 (读 raw + canonical 计数; closed-loop gate)
  - org_holding_pointer_integrity.py  # 2026-07-27: accepted pointer vs full canonical COUNT (class-A merge guard)
  - type_b_fact_publish_catchup.py  # B2 2026-07-24: same-run bounded raw→fact publish after registry drain
  - residual_hygiene.py  # F9 2026-07-26: Type-B publish lag + ann tip vs eligible SLA (audit; store 2.985 + type_b post-evaluate; not SERVE consumer)
  - org_holding_period_catchup.py  # org 中间季 bounded fill N=1/run (plan_partition_catchup + sync_period)
  - qfii_client.py           # QFII 采集 (aif10 sanctioned 源 §4.3; 读 raw_qfii_holding_quarterly 期次存在)

# 源退役中 = 临时成员 (删源程序 task#56 完成后随 client 退役; 现不算违规, 但标记为待退役)
# source_retiring_temp_members 已全退役完 2026-06-29 (批2 收口): 所有非 tushare/非 sanctioned-aif10 源退役。
#   tdx_affair_client(2026-06-27 gpcw) / tdx_f10_extra_client(2026-06-24 tdx F10) /
#   institution_survey_client(2026-06-29 批2 切 tushare stk_surv, aif10+akshare 退役) /
#   financial_client(2026-06-28, 财务走 tushare sync 域) / capital_client(2026-06-27 akshare 资本运作 cut)。
source_retiring_temp_members: []
