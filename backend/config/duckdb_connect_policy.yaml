version: 1

db_path_literal_policy:
  block_data_duckdb_literals: true
  database_manifest_path: database_manifest.yaml

# raw duckdb.connect 白名单 (check_rule_compliance 消费): 这些文件文件级直接控制是设计, 不走 adapter。
# 2026-06-28 prune: 89→7 (删 82 个引已删模块/脚本的死路径; 纯数据平台重建后大批策略/特征/回测脚本退役)。
# 新增 raw connect 默认阻断, 须显式加这里 + 理由 (或改用 services.duck_adapter.connect / resolver.connect_ro)。
allowed_raw_connect_paths:
  - scripts/migrate_reference_db.py        # §9 reference 拆库迁移 (跨库 ATTACH/EXPORT 保真, 2026-06-24)
  - services/data_audit.py                 # post-sync audit 多库 read_only ATTACH
  - services/duck_adapter.py               # adapter 本体
  - scripts/audit_data_completeness.py     # 数据完整性审计 (read_only)
  - scripts/audit_pit_coverage.py          # PIT 覆盖审计 (read_only)
  - scripts/build_dim_listing_status.py    # 退市状态 dim 写 reference RW
  - scripts/cleanup_holder_dup.py          # holder 去重维护
