version: 1

# 当前 qfq 分析序列只由 TuShare raw+adj_factor 派生，无跨供应商 fallback。
# 它是 research/serving read surface，不是 nominal execution truth，也不是 Tier0 AcceptedPartition。
capabilities:
  kline_daily:
    primary: tushare
    fallback: []
    analysis_relation: market.v_price_kline_qfq
    allow_fallback_for_latest_gap: false
    require_fallback_lineage: false
    max_primary_lag_trading_days: 1
