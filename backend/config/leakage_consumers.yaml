# 泄漏检测消费者注册表 — 所有"在特征面板上训练/打分模型"的脚本必须在此登记 (2026-06-13)
#
# 为什么: 工具不强制 = 孤儿 (mythos #14)。S3 漏排 follow_net_return 致 AUC 0.84 假象, 靠的是
# 作者手写 EXCLUDE 自觉; live 碰巧干净也是运气。强制三道闸:
#   (1) 此注册表 = 真相源 (谁在用特征面板)
#   (2) leakage_probe --gate 遍历本表逐个跑事前探针 (接 doctor, 每次全面检测)
#   (3) safe_commit 拦截: 改了 builder/消费者/新训练脚本 → 跑 --gate; 新训练脚本未登记 → 阻断
#
# 字段: id / script / panel (库内表) / label_col (二分或连续>0 二分) / exclude_source (标签契约 builder, 空=仅名模式+AUC)
# verdict HIGH 任一 = gate FAIL。新增模型训练/打分脚本必须加条目 (否则 safe_commit 阻断)。
#
# 2026-06-15 post-reset 注册表对账 (用户"做好数据底座" + §4.4 root cause): 地基-reset (639e0dfb 删771)
#   物删了全部 3 个历史消费者脚本 + 其特征面板, 注册表未同步 = 漂移 (gate 扫已删 panel 的 dim 血缘误 BLOCK):
#     - zhushenglang_s3        (experiment_zhushenglang_s3.py DELETED; panel fact_feature_panel 已重建但 schema 变=纯5因子无 forward_ret label)
#     - v7_daily_inference     (run_daily_v7_inference.py DELETED; panel mart_p0a_feature_label_panel_v5 WIPED)
#     - p0b_lambdamart_v6      (run_p0b_lambdamart_v6.py DELETED; panel mart_p0a_feature_label_panel_v4 WIPED)
#   三者全退役。当前 live 训练/打分消费者 = 0 (重建中)。**新建的多因子探索 runner 消费 fact_feature_panel
#   时必须在此登记** (其 label=回测前向收益, 走 leakage_probe --gate 事前探针)。fact_feature_panel 本身
#   是纯 PIT 因子面板 (mom_60/reversal_20/vol_20/mf_trend_20/roe_dt_asof, 无 forward-return 标签列), 无标签透传面。
version: 1
db: data/feature_store.duckdb   # L2 面板主库 (2026-06-15: fact_feature_panel 迁此; 历史 smartmoney 面板已 wiped)
since: "2025-01-01"          # 探针采样窗 (避开早期稀疏; 与训练窗对齐)
consumers: []                # post-reset: 0 live 消费者; 新 runner 消费 fact_feature_panel 时登记 (gate 事前探针)

# audit_panel_leakage.py 的 panel-build PIT 审计目标 (能不硬编码就不硬编码 — 之前工具硬编码 default=
# mart_p0a_feature_label_panel_v4[已wipe] + build_feature_panel_duck.py[已删], 2026-06-15 改 config 驱动)。
# 每项 {panel, db(相对repo路径), build_sql(相对repo路径)}; 工具审 build-SQL 的 JOIN PIT-strict + dim 标记 +
# 生存者偏差等 9 检 (针对 **SQL-built** 面板)。
# post-reset 空: 旧 SQL 面板 (mart_p0a_v4/v5) 已 wipe; 现 L2 fact_feature_panel 是 **Python builder**
# (build_feature_panel.py, 因子在 Python 算非 SQL JOIN) + code/date schema, 此 SQL-JOIN 审计工具不适用
# (它的 PIT 控制在 build 时 + moth feature-panel-l2-built + 同日截面组合)。新 SQL-built 面板在此登记后自动纳审。
audit_panels: []
