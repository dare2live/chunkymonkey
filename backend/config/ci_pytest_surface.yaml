version: 1
blocking_paths:
- tests/scripts/test_agent_board_projection.py
- tests/scripts/test_agent_boot.py
- tests/scripts/test_audit_date_bounds.py
- tests/scripts/test_audit_exemptions.py
- tests/scripts/test_audit_min_rows_baseline.py
- tests/scripts/test_build_feature_map_drift.py
- tests/scripts/test_build_price_kline_qfq_tushare.py
- tests/scripts/test_build_sw_industry_view.py
- tests/scripts/test_check_calendar_usage.py
- tests/scripts/test_check_codex_review.py
- tests/scripts/test_check_commit_message.py
- tests/scripts/test_check_doc_runtime_state.py
- tests/scripts/test_check_foundation_done.py
- tests/scripts/test_check_staged_blob_size.py
- tests/scripts/test_check_strategy_lab.py
- tests/scripts/test_check_universe_filter.py
- tests/scripts/test_ci_paths_policy.py
- tests/scripts/test_ci_pytest_surface_drift.py
- tests/scripts/test_classify_commit_tier.py
- tests/scripts/test_completeness_ref.py
- tests/scripts/test_cross_section_full.py
- tests/scripts/test_db_compact.py
- tests/scripts/test_db_split_org_holding.py
- tests/scripts/test_dip_scan.py
- tests/scripts/test_dip_severity.py
- tests/scripts/test_downstream_impact.py
- tests/scripts/test_ensure_serve_read_views.py
- tests/scripts/test_exemption_integrity.py
- tests/scripts/test_governance_gates.py
- tests/scripts/test_history_cli.py
- tests/scripts/test_manual_job_wrapper.py
- tests/scripts/test_persist_phase_d_experiment_runs.py
- tests/scripts/test_pre_knife_audit.py
- tests/scripts/test_run_ci_pytest.py
- tests/scripts/test_safe_commit.py
- tests/scripts/test_verified_low_days.py
- tests/services/test_accepted_schema.py
- tests/services/test_acquire_bounded_gap_heal.py
- tests/services/test_assignment_gap_recon.py
- tests/services/test_availability.py
- tests/services/test_baostock_adapter.py
- tests/services/test_baostock_session_lock.py
- tests/services/test_brick_registry_b5.py
- tests/services/test_calendar_acceptance.py
- tests/services/test_calendar_contract.py
- tests/services/test_calendar_identity_recon.py
- tests/services/test_cyq_recon.py
- tests/services/test_dataset_contracts.py
- tests/services/test_dc_member_publish_b1.py
- tests/services/test_derive_runtime_s5.py
- tests/services/test_disclosure_boundaries.py
- tests/services/test_disclosure_dual_write.py
- tests/services/test_disclosure_enrichment_projection.py
- tests/services/test_disclosure_research_read.py
- tests/services/test_disclosure_shadow_compare.py
- tests/services/test_disclosure_transport_e0.py
- tests/services/test_dual_plane_faucet.py
- tests/services/test_duckdb_compact.py
- tests/services/test_factor_family_continuity_gates.py
- tests/services/test_factor_family_frontier_projection.py
- tests/services/test_factor_family_inventory_gate.py
- tests/services/test_fina_margin_recon.py
- tests/services/test_formal_boundaries.py
- tests/services/test_formal_execution.py
- tests/services/test_formal_rx_evidence.py
- tests/services/test_formula_challenge.py
- tests/services/test_foundation_obs_serve.py
- tests/services/test_frontier_decision.py
- tests/services/test_fuyao_k5_adapter.py
- tests/services/test_fuyao_kline_recon.py
- tests/services/test_holder_research_class.py
- tests/services/test_holdernumber_assist.py
- tests/services/test_holders_landing_retention.py
- tests/services/test_holders_top10_acceptance.py
- tests/services/test_index_daily_publish_b2.py
- tests/services/test_institution_follow_b0.py
- tests/services/test_institution_follow_b1.py
- tests/services/test_institution_follow_b2.py
- tests/services/test_institution_follow_b4.py
- tests/services/test_institution_follow_spec_paper.py
- tests/services/test_legacy_raw_plane_s7.py
- tests/services/test_main_rally_b0.py
- tests/services/test_main_rally_b1.py
- tests/services/test_main_rally_b2.py
- tests/services/test_main_rally_setup_paper.py
- tests/services/test_manual_probe_first_clock.py
- tests/services/test_margin_acceptance.py
- tests/services/test_margin_catchup.py
- tests/services/test_margin_population_scope.py
- tests/services/test_margin_pulse_promote_gate.py
- tests/services/test_market_pulse_serve_read_s6.py
- tests/services/test_moneyflow_recon.py
- tests/services/test_northbound_market_flow.py
- tests/services/test_notification_dispatcher_skip_macos.py
- tests/services/test_observation_population.py
- tests/services/test_org_holding_acceptance.py
- tests/services/test_org_holding_announcement.py
- tests/services/test_org_holding_db.py
- tests/services/test_org_holding_period_catchup.py
- tests/services/test_org_holding_pointer_integrity.py
- tests/services/test_org_holding_truncation_audit.py
- tests/services/test_pagination_integrity.py
- tests/services/test_periodic_report_calendar.py
- tests/services/test_phase_e_smoke.py
- tests/services/test_population_scope.py
- tests/services/test_project_status.py
- tests/services/test_registry_merge_chain_single_source.py
- tests/services/test_research_identity_overlays.py
- tests/services/test_research_prereg_store.py
- tests/services/test_research_runtime.py
- tests/services/test_residual_hygiene.py
- tests/services/test_run_outcome.py
- tests/services/test_runtime_limits.py
- tests/services/test_security_day_acquire_s4.py
- tests/services/test_security_day_transport_modularity.py
- tests/services/test_sibling_repos.py
- tests/services/test_snapshot_nominal_bind.py
- tests/services/test_stk_holdertrade_acceptance.py
- tests/services/test_stk_holdertrade_catchup.py
- tests/services/test_stock_limit_publish_b2.py
- tests/services/test_stock_moneyflow_publish_b2.py
- tests/services/test_strategy_lab.py
- tests/services/test_strategy_lab_serve.py
- tests/services/test_strategy_paper.py
- tests/services/test_strategy_spec.py
- tests/services/test_structure_product_recon.py
- tests/services/test_sync_execution_policy.py
- tests/services/test_sync_runner_20260612_fixes.py
- tests/services/test_taxonomy_recon.py
- tests/services/test_tdxhub_history_pagination.py
- tests/services/test_tdxhub_host_memory.py
- tests/services/test_tdxhub_kline_recon.py
- tests/services/test_tdxhub_mac_capital_flow.py
- tests/services/test_tdxhub_source_adapter.py
- tests/services/test_tier12_project_universe.py
- tests/services/test_tier12_publish_accept.py
- tests/services/test_tier12_publish_contract.py
- tests/services/test_tier12_publish_scope.py
- tests/services/test_tier12_publish_writer.py
- tests/services/test_top_inst_seat_publish_b2.py
- tests/services/test_type_b_fact_publish_catchup.py
- tests/services/test_universe_st_calendar.py
- tests/test_by_ann_date_equal_day_reprobe.py
- tests/test_calendar_gate.py
- tests/test_check_dead_references.py
- tests/test_check_doc_governance.py
- tests/test_cx3_capability_bricks.py
- tests/test_db.py
- tests/test_decision_intersection.py
- tests/test_foundation_obs_api.py
- tests/test_frontend_site.py
- tests/test_holdout_guard.py
- tests/test_moneyflow_assist.py
- tests/test_on_demand_window_gate_dead_condition.py
- tests/test_pipeline_closed_loop.py
- tests/test_pipeline_delta_manifest.py
- tests/test_pipeline_state_sensors.py
- tests/test_primitives.py
- tests/test_source_watermarks.py
- tests/test_stock_dossier_api.py
- tests/test_stock_screener.py
- tests/test_strategy_lab_api.py
- tests/test_universe.py
- tests/test_utils.py
nightly_paths: []
ci_test_optional:
- path: tests/data_governance/test_etl_hook.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/realdb/test_real_data_consistency.py
  reason: realdb marker -- requires live production DuckDB fixtures; not offline-CI-safe
- path: tests/scripts/test_audit_data_completeness.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_build_dc_industry_view.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_check_continuity_integrity.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_check_continuity_integrity_field_ownership.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_check_grain_uniqueness.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_chunkyctl_doctor.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_chunkyctl_wrapper.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_db_lifecycle_delete.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/scripts/test_perf_p1_trade_date.py
  reason: perf+slow marker -- timing-budget test, opt-in only (pytest.ini excludes by default)
- path: tests/scripts/test_update_watermark_sla.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_batch_integrity.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_calendar_reader.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_calendar_runtime.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_calendar_schema.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_database_manifest.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_disclosure_dataset_snapshot.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_lineage.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_margin_projections.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_margin_reconcile.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_margin_state.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_market_pulse_scope.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_market_pulse_shadow_reconcile.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_market_pulse_tier12_read.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_nominal_ohlcv_acceptance.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_persist_tier12_cutover_aware.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_sandbox_guard.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_sync_preconditions.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_sync_runner_drain.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_sync_runner_integrity.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_sync_runner_today_catchup.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_sync_runner_watermark_decouple.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_tier12_consumer_cutover.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_tier12_form_enrich.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_tier12_nominal_canary.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_universe_serve_filter.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/services/test_writer_lock.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_acquire_active_stock_refresh.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_by_ann_date_weekend_coverage.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_by_trade_date_explicit_start_range.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_by_trade_date_fixed_params.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_by_trade_date_split_by.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_calendar_builder.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_check_doc_drift.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_check_sandbox_isolation.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_check_serve_read_layer.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_continuity_guard.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_data_access.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_data_audit_kline_completeness.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_db_health.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_dependency_guards.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_holders_aif10.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_institution_profile.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_institution_profile_api.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_kline_write_calendar_lint.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_market_db_canonical_kline.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_market_pulse.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_market_pulse_api.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_mart_data_lineage_compat.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_min_rows_era_aware.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_min_rows_per_batch_threshold.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_ops_manual_run.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_org_holding_aif10.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_paper_portfolio.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_pipeline.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_pipeline_stage_status.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_qfii_client.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_rally_gt.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_segments.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_source_policy.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_sync_cross_check.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_sync_increment.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_sync_runner_ratelimit.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_system_routes.py
  reason: realdb marker -- requires live production DuckDB fixtures; not offline-CI-safe
- path: tests/test_taxonomy_config.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_technical_states.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
- path: tests/test_tushare_authorization.py
  reason: 'pre-existing CI-list gap surfaced by the 2026-07-20 Fable5 CI-tax audit (binding finding #2:
    ci.yml pytest list had drifted from the committed backend/tests/**/test_*.py surface); not triaged
    for offline-CI safety in this change, so not silently added -- promote explicitly after a live-dependency
    review, one triage slice at a time'
