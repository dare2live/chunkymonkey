version: 2
status: training_boundary_only

# Active owner: services/holdout_guard.py. The date is the first reserved holdout
# day, so every training/tuning data_end_date must be strictly earlier.
holdout_start: "20250601"

# Preregistration, global single-touch, parameter freeze and StrategyRelease are
# requirements in docs/strategy_validation_contract.md, not active YAML/runtime
# claims. Add them here only when an atomic Tier3 evidence store and concurrent-
# writer regression tests exist.
