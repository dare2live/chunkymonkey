version: 1

defaults:
  mode: dry_run
  require_explicit_execute_flag: true
  large_delete_row_threshold: 1000000

payload_audit:
  max_value_warn_bytes: 1048576
  max_value_fail_bytes: 10485760
  total_value_warn_bytes: 10485760
  total_value_fail_bytes: 104857600
  top_sample_rows: 20
  duplicate_sample_warn_count: 2
  fail_on_recursive_keyword: true
  warn_on_path_marker: true
  payload_column_name_tokens:
    - json
    - payload
    - audit
    - evidence
    - summary
    - signals
    - blob
    - bytes
    - raw_text
    - raw_json
    - raw_bytes
    - command
    - path
    - url
    - artifact
    - details
    - preview
    - warning
    - blocker
    - reason
    - lineage
  recursive_keywords:
    - latest_dispatch
    - queue_items
    - audit_json
    - signals_json
    - preview
  path_markers:
    - data/reports/
    - analysis/
    - /logs/
    - .log
    - file://
  reviewed_columns:
    - table: fact_technical_trigger
      column: reason_codes_json
      classification: compact_per_signal_reason_codes
      owner: formula_engine
      reason: Full-history formula trigger reason-code evidence; row count can grow with signal history, but each value must stay tiny and non-recursive.
      max_value_bytes: 512
      max_total_value_bytes: 536870912
    - table: mart_macd_state_history
      column: reason_codes_json
      classification: diagnostic_state_history_evidence
      owner: formula_engine
      reason: MACD state history is evidence-only; per-row reason codes are bounded and intentionally retained for candidate-supply audits, so aggregate volume can grow with history without becoming recursive.
      max_value_bytes: 512
      max_total_value_bytes: 536870912
    - table: mart_stock_picture_daily
      column: institution_top_json
      classification: bounded_picture_daily_institution_summary
      owner: picture_daily
      reason: Daily stock picture keeps a compact top-institution summary per stock/date for UI and recommendation evidence; it must remain bounded and non-recursive.
      max_value_bytes: 2048
      max_total_value_bytes: 67108864
    - table: mart_today_signal_cache_signal
      column: signal_json
      classification: bounded_cache_detail_row
      owner: signals_v2
      reason: Replacement for the former single-row signals_json blob; one bounded JSON row per signal keeps API cache replayable without recursive nesting.
      max_value_bytes: 8192
      max_total_value_bytes: 67108864
    - table: raw_aif10_financial_history
      column: raw_json
      classification: bounded_raw_source_record
      owner: financial_source_ingestion
      reason: AIF10 source snapshots are replayable raw inputs; large total volume is expected only when each source record remains bounded.
      max_value_bytes: 16384
      max_total_value_bytes: 67108864
    - table: raw_tdx_f10_holder_research
      column: raw_text
      classification: bounded_raw_source_record
      owner: tdx_f10_source_ingestion
      reason: TDX F10 holder raw text is a protected replay source; total volume is acceptable only with bounded individual records and zero recursive keys.
      max_value_bytes: 65536
      max_total_value_bytes: 1073741824
    - table: raw_tdx_gpcw_wide
      column: field_values_json
      classification: bounded_raw_source_record
      owner: tdx_gpcw_source_ingestion
      reason: TDX GPCW wide field JSON is a protected replay source; total volume is acceptable only with bounded individual records and zero recursive keys.
      max_value_bytes: 8192
      max_total_value_bytes: 536870912
    - table: mart_architecture_inventory_asset
      column: api_routes_json
      classification: governed_architecture_inventory_paths
      owner: architecture_inventory
      reason: Architecture inventory intentionally records route/path references; path markers are lineage evidence, not embedded artifacts.
      allow_path_marker: true
      max_value_bytes: 4096
      max_total_value_bytes: 1048576
    - table: mart_architecture_inventory_asset
      column: frontend_api_calls_json
      classification: governed_architecture_inventory_paths
      owner: architecture_inventory
      reason: Architecture inventory intentionally records frontend API path references; path markers are lineage evidence, not embedded artifacts.
      allow_path_marker: true
      max_value_bytes: 4096
      max_total_value_bytes: 1048576
    - table: mart_paper_sim_kpi
      column: lineage_url
      classification: governed_lineage_pointer
      owner: paper_sim_lineage
      reason: Lineage URL is an intentional pointer to a report artifact, not a copied artifact payload.
      allow_path_marker: true
      max_value_bytes: 512
      max_total_value_bytes: 1048576
    - table: mart_strategy_result_registry
      column: evidence_json
      classification: governed_strategy_evidence_pointer
      owner: strategy_result_registry
      reason: Strategy evidence can include artifact path references; recursive audit payloads remain blocked by recursive-key detection.
      allow_path_marker: true
      max_value_bytes: 4096
      max_total_value_bytes: 1048576
    - table: mart_strategy_result_registry
      column: lineage_url
      classification: governed_lineage_pointer
      owner: strategy_result_registry
      reason: Lineage URL is an intentional pointer to a report artifact, not a copied artifact payload.
      allow_path_marker: true
      max_value_bytes: 512
      max_total_value_bytes: 1048576
    - table: mart_stock_formula_optuna_bestchoice_v1
      column: source_artifact
      classification: governed_source_artifact_pointer
      owner: bestchoice_phase1_import
      reason: BestChoice candidate rows store source artifact pointers for reproducibility; they must remain short pointers, not embedded artifacts.
      allow_path_marker: true
      max_value_bytes: 512
      max_total_value_bytes: 1048576

protected_model_statuses:
  - champion
  - challenger
  - shadow

candidate_feature_panels:
  - table: fact_feature_panel_candidate
    key_column: feature_set_id
    built_at_column: built_at
    retain_latest_keys: 3
    protect_if_referenced_by_lifecycle_training_config: true
    protect_if_referenced_by_model_selection_run: true

model_prediction_tables:
  - table: mart_multidim_prediction
    model_id_column: model_id
  - table: mart_daily_recommendation
    model_id_column: model_id
  - table: mart_daily_topk_view_cache
    model_id_column: model_id
  - table: mart_daily_recommendation_risk
    model_id_column: model_id
  - table: mart_model_explanation
    model_id_column: model_id
  - table: mart_daily_recommendation_explanation
    model_id_column: model_id
  - table: mart_model_walkforward_prediction
    run_id_column: run_id
    model_id_source_table: mart_model_walkforward_fold
    model_id_source_run_id_column: run_id
    model_id_source_model_id_column: model_id

protected_artifact_tables:
  - table: raw_tdx_f10_holder_research
    reason: TDX F10 raw holder snapshots are replayable source records
  - table: raw_tdx_f10_holder_count_history
    reason: TDX F10 parsed raw holder-count snapshots support source-date replay
  - table: raw_tdx_f10_extra_parse_status
    reason: TDX F10 parse ledger prevents repeated failed or terminal parses
  - table: mart_tdx_f10_capability_matrix
    reason: TDX F10 capability and PIT-date matrix is source governance evidence
  - table: mart_feature_pit_audit
    reason: PIT audit rows gate feature and model promotion
  - table: mart_feature_pit_coverage_summary
    reason: high/critical PIT coverage summary is model governance evidence
  - table: mart_stock_horizon_selection
    reason: per-stock holding horizon selection is recommendation evidence

table_inventory:
  - table: raw_tdx_f10_holder_research
    classification: protected_raw_lineage
    owner: tdx_f10_source_ingestion
    db_alias: smartmoney
    truth_source: tdx_f10_raw_holder_snapshot
    consumers:
      - holder_replay
      - holder_count_pit_lineage
      - data_health_source_audit
    compaction_policy: protect_no_compaction_without_verified_copy
    retention_action: protect_replayability_before_any_retention_change
    reason: Large F10 raw-text table; needed for raw_hash replay, PIT lineage, and canonical holder fact explainability.
  - table: fact_feature_panel
    classification: current_production_panel
    owner: feature_panel_pipeline
    db_alias: smartmoney
    truth_source: kline_calendar_and_feature_pipeline_manifest
    consumers:
      - feature_join
      - daily_recommendation
      - model_training
      - workbench_feature_catalog
    compaction_policy: protect_no_compaction_without_verified_copy
    retention_action: protect_current_canonical_panel
    reason: Canonical feature panel backed by K-line truth-source lineage; not a cache cleanup candidate.
  - table: mart_p0a_feature_label_panel_v4
    classification: current_production_panel
    owner: panel_pipeline_manifest
    db_alias: smartmoney
    truth_source: panel_pipeline_manifest_current_retrain_panel
    consumers:
      - retrain_pipeline
      - sniper_score
      - institution_score
      - panel_lineage_audit
    compaction_policy: protect_no_compaction_without_verified_copy
    retention_action: protect_current_retrain_panel
    reason: Current P0A feature-label panel used by retrain/sniper/institution flows.
  - table: mart_p0a_feature_label_panel_v3
    classification: upstream_reproducibility_evidence
    owner: panel_pipeline_manifest
    db_alias: smartmoney
    truth_source: panel_pipeline_manifest_v4_input
    consumers:
      - p0a_v4_reproducibility
      - panel_lineage_audit
    delete_gates:
      - codegraph_no_live_consumer
      - pipeline_manifest_rebuild_path_verified
      - row_count_and_schema_snapshot_saved
    rollback_evidence:
      - copied_duckdb_execute_dry_run
      - panel_pipeline_manifest_hash
    compaction_policy: checkpoint_after_approved_delete
    retention_action: classify_consumer_before_cleanup
    reason: Upstream input for v4 in panel pipeline manifest; cleanup requires reproducibility owner review.
  - table: mart_p0a_feature_label_panel_v5
    classification: protected_runtime_panel
    owner: panel_pipeline_manifest
    db_alias: smartmoney
    truth_source: historical_panel_pipeline_output
    consumers:
      - runtime_consumer_v7_daily_inference
      - unified_panel_builder
      - feature_join_v5_pipeline
      - historical_reproducibility
    delete_gates:
      - v7_daily_inference_migrated_or_retired
      - unified_panel_builder_migrated_or_retired
      - codegraph_no_live_consumer_after_deprecation
      - rg_no_runtime_reference
      - pipeline_manifest_rebuild_path_verified
      - row_count_and_schema_snapshot_saved
    rollback_evidence:
      - copied_duckdb_execute_dry_run
      - deletion_manifest_with_counts
    compaction_policy: protect_no_compaction_without_verified_copy
    retention_action: protect_until_runtime_consumers_are_migrated_or_retired
    reason: Full-width panel version overlaps unified_v1 keys but still feeds v7 daily inference and unified panel build paths.
  - table: mart_p0a_feature_label_panel_unified_v1
    classification: protected_research_panel
    owner: panel_pipeline_manifest
    db_alias: smartmoney
    truth_source: historical_panel_pipeline_output
    consumers:
      - unified_ranker_train_smoke
      - unified_ranker_walkforward
      - phase42_diag_ablation
      - historical_reproducibility
    delete_gates:
      - unified_ranker_scripts_retired_or_migrated
      - codegraph_no_live_consumer_after_deprecation
      - rg_no_runtime_reference
      - pipeline_manifest_rebuild_path_verified
      - row_count_and_schema_snapshot_saved
    rollback_evidence:
      - copied_duckdb_execute_dry_run
      - deletion_manifest_with_counts
    compaction_policy: protect_no_compaction_without_verified_copy
    retention_action: protect_until_research_consumers_are_retired_or_archived
    reason: Full-width panel version overlaps v5 keys but still has training, walk-forward, and diagnostic script consumers.
  - table: mart_p0a_feature_label_panel
    classification: protected_baseline_panel
    owner: panel_pipeline_manifest
    db_alias: smartmoney
    truth_source: legacy_panel_pipeline_output
    consumers:
      - p0b_lightgbm_default
      - p1_ablation_default
      - p0a_panel_audit_default
      - schema_and_field_dictionary
      - historical_reproducibility
    delete_gates:
      - legacy_cli_defaults_migrated_or_retired
      - schema_and_field_dictionary_repointed
      - codegraph_no_live_consumer_after_deprecation
      - rg_no_runtime_reference
      - row_count_and_schema_snapshot_saved
    rollback_evidence:
      - copied_duckdb_execute_dry_run
      - deletion_manifest_with_counts
    compaction_policy: protect_no_compaction_without_verified_copy
    retention_action: protect_until_cli_defaults_and_schema_refs_are_migrated
    reason: Baseline panel consumes material space but remains the default input for P0b training, P1 ablation, and P0a audit scripts.
  - table: fact_feature_panel_candidate
    classification: governed_candidate_panel
    owner: storage_retention_policy
    db_alias: smartmoney
    truth_source: candidate_feature_panel_retention_rules
    consumers:
      - model_selection_run
      - model_lifecycle_training_config
      - feature_pit_audit
      - research_schedule_plan
    delete_gates:
      - protected_feature_set_reason_scan
      - retain_latest_keys_policy
      - copied_duckdb_execute_dry_run
    rollback_evidence:
      - data_deletion_manifest
      - copied_duckdb_before_after_counts
    compaction_policy: checkpoint_after_approved_delete
    retention_action: use_candidate_feature_panel_retention_rules
    reason: Candidate feature-set table already has key-level retention rules; current dry-run may still protect all keys.
  - table: fact_feature_panel_tdx_keep_challenger
    classification: derived_challenger_panel_requires_owner
    owner: tdx_keep_challenger_pipeline
    db_alias: smartmoney
    truth_source: tdx_keep_challenger_pipeline_output
    consumers:
      - tdx_keep_promotion_gate
      - challenger_evidence_bundle
      - historical_reproducibility
    delete_gates:
      - codegraph_no_live_consumer
      - promotion_gate_reference_check
      - rebuild_command_verified
      - row_count_and_schema_snapshot_saved
    rollback_evidence:
      - copied_duckdb_execute_dry_run
      - challenger_artifact_manifest
    compaction_policy: checkpoint_after_approved_delete
    retention_action: prove_regenerability_and_consumer_status_before_delete
    reason: Derived challenger panel likely regenerable, but tied to challenger evidence and promotion flows.
  - table: mart_feature_rank_matrix_cache_manifest
    classification: cache_manifest_evidence
    owner: feature_rank_matrix_cache
    db_alias: smartmoney
    truth_source: feature_rank_matrix_cache_identity_ledger
    consumers:
      - feature_rank_matrix_cache
      - storage_retention_policy
      - workbench_storage_view
    delete_gates:
      - cache_policy_replacement_verified
      - no_cache_tables_depend_on_manifest
    rollback_evidence:
      - copied_duckdb_execute_dry_run
      - cache_manifest_export
    compaction_policy: protect_until_cache_policy_then_checkpoint
    retention_action: protect_until_cache_policy_uses_manifest
    reason: Manifest records rank-matrix cache identity and usage; keep it separate from cache table deletion candidates.
  - table_like: mart_feature_rank_matrix_cache_%
    exclude_tables:
      - mart_feature_rank_matrix_cache_manifest
    classification: cache_candidate
    owner: feature_rank_matrix_cache
    db_alias: smartmoney
    truth_source: feature_rank_matrix_cache_manifest
    consumers:
      - feature_rank_matrix_cache
      - workbench_storage_view
    delete_gates:
      - cache_manifest_stale_key_policy
      - codegraph_no_direct_table_consumer
      - copied_duckdb_execute_dry_run
    rollback_evidence:
      - cache_manifest_export
      - copied_duckdb_before_after_counts
    compaction_policy: checkpoint_after_cache_policy_delete
    retention_action: define_stale_cache_policy_before_delete
    reason: Persistent rank-matrix cache tables overlap candidate panel keys; delete only after cache manifest and consumer checks.

model_file_roots:
  - data/multidim_models

optuna_study_roots:
  - data/optuna
