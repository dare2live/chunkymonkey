# 核心依赖
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.24.0
# akshare/tushare SDK 已退役；生产采集唯一入口 TuShareSource 使用 tinyshare 兼容代理。
tinyshare==0.1030.0
numpy
pydantic>=2.0
PyYAML>=6.0

# Legacy compatibility dependency for retained TDX-backed modules; it is not the
# Tier0 market-data owner. Current acquisition ownership is declared by sync contracts.
tdxhub @ git+https://github.com/dare2live/tdxhub.git@b039d8c68fb21543a9ff041f004d75113b5a4e3d

# AI 模型 (LightGBM + Optuna 多维评分)
lightgbm
optuna
duckdb
scipy
scikit-learn
pytest-asyncio
