# ChunkyMonkey Goal

> 本文件只写**项目要达成什么**与**怎么算达成了**。
> 进度/backlog 查 `scripts/chunkyctl status` 与 `chunkyctl history --grep <关键词>`（git log 即原件）。
> 法条与契约只认 `docs/README.md` 指向的 owner contracts；本文件不留副本（副本必然与 owner 漂移）。

## 北极星

**主升浪猎手**：在个股启动主升浪的早期识别它，并给出可执行的买卖点。
达成判据 = 一个 `StrategyRelease` 通过 `strategy_validation_contract.md` 的 B0→B5 消融阶梯，
且其超额收益在 opaque holdout 上 `claimable=true`。**当前没有任何策略达到，这是唯一的终局目标。**

## 产品要回答的三问

1. **市场在哪** —— 当下资金与情绪在哪些板块/概念聚集。
2. **谁在买** —— 机构、游资、北向、散户的分层行为证据（观点类数据须标体系名，跨体系不可冒充等价）。
3. **价量结构成不成立** —— 个股的形态/量能/筹码是否支持一次启动。

达成判据 = 三问各有 serve 层 entity 支撑、有前端消费方、且每个观测维度可溯源到一手真相源。

## 研究范式（这是定义，不是偏好）

**信号条件有效性分层**：公式发出买卖信号 → 按信号日**可见**的观测量分桶 → 看每桶前向收益与胜率。

- 四层：信号事件流 → 观测维度字典（信号日可见）→ 前向收益（`next_tradable_open`）→ 分层胜率表。
- **不做**多因子建模、不加权、不回归、不寻优、不预测。`factor_family_inventory.yaml` 是**分层字典的目录**，不是模型输入。
- 结果倒推（先知道赢家再看当时有什么特征），不是信号正推（先造信号再找收益）。

达成判据 = 每个结论都能回答"哪一步、是否 D1 落库、是结果倒推吗"。

## 不可回滚的约束

- **PIT 正确**：任何观测量必须声明"何时可见"，禁用法定截止日冒充已知日；违反即作废，不论结果多好。
- **真相源优先**：判断锚在第一手源（原始数据/官方规则/交易所公告），不是派生表或供应商的算好值。
- **可推导优先于取数**：能由「已注册域 + 公开规则」得到的，不注册取数域；推导物必须锁规则有效期，过期 fail 不 warn。
- **诚实高于完整**：`unknown`/`partial`/`measured reject` 留着就是做对了，禁为清单洗绿。
- **单机 + 单一计算点**：派生事实只算一次；拒绝为通用而通用的抽象层。

## 边界（明确不做）

不做高频/日内、不做衍生品、不做实盘自动下单、不做多因子打分模型、不做通用回测框架。
