# ChunkyMonkey Goal

> 只写项目要达成什么、怎么算达成。进度查 `scripts/chunkyctl status`；历史查 `scripts/chunkyctl history --grep <词>`（git 即原件）。
> 规则不在这里：跨项目的在 `~/.claude/CLAUDE.md` 与 mio skill，本项目的在根目录 `CLAUDE.md`，机器执法的在 `backend/config/governance_gates.yaml` 每道门的 `invariant`。

## 北极星

**主升浪猎手**：在个股启动主升浪的早期识别它，并给出可执行的买卖点。

**这是策略验证项目，不是量化项目。** 策略是人给的输入（手写选股公式）；系统不产出策略、不拟合参数、不判定策略"够不够格"。系统只回答：**它历史上每次触发之后发生了什么，在什么情形下表现更好。**

达成判据：任一手写公式都能被验证——在全历史全宇宙查出它的触发点 → 按信号日**可见**的观测量分桶给出前向收益与胜率 → 回答"今天这个信号落在历史上哪个情景"。两条底线是判据的一部分：**格内先例不足时报「先例不足」而不是给数字**；**不匹配任何历史情景时明说不匹配**。

## 产品要回答的三问

1. **市场在哪** —— 当下资金与情绪在哪些板块/概念聚集。
2. **谁在买** —— 机构、游资、北向、散户的分层行为证据（观点类数据须标体系名，跨体系不可冒充等价）。
3. **价量结构成不成立** —— 个股的形态/量能/筹码是否支持一次启动。

达成判据：三问各有 serve 层 entity 支撑、有前端消费方、且每个观测维度可溯源到一手真相源。

## 判例引擎要交付什么

四层：信号事件流 → 观测维度字典（信号日可见）→ 前向收益（`next_tradable_open`）→ 分层胜率表。
每条公式共享同一 universe / 交易日历 / 名义可成交判定 / 前向窗口；公式是输入，不被拟合。
结果倒推（先知道赢家再看当时有什么特征），不是信号正推。

达成判据：每个结论都能回答"哪一步产生、落库了没有、是结果倒推吗"；每个数字可追到公式 id、情形维度、样本量与置信区间。

## 数据层要达到什么

派生 ← 接受 ← 证据，每层可从下一层一键重生成且 hash 相等；每个落盘的可重生成物记录何时、用什么配置、从哪些输入算的，输入在它之后更新即过期即门红；一个 dataset 的事实属性只写在一处登记表，门读它。

达成判据：登记表门 0 warn（无幽灵表、无孤儿表）；`derived_stale` 0 FAIL；同一份数据物理只有一份。

## 边界（明确不做）

不做高频/日内、不做衍生品、不做实盘自动下单、不做多因子打分模型（不加权、不回归、不寻优、不预测）、不做通用回测框架、不建通达信公式求值器（公式在对话里转换后再进系统）。
