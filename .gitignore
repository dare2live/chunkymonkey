.DS_Store
*.db
*.db-journal
*.db-wal
*.db-shm
__pycache__/
*.pyc
Users/
data/multidim_models/*.pkl
data/optuna/
data/storage_cleanup_smoke/
data/audit/
.codegraph/
mlruns/
node_modules/
.env
logs/
backend/forecast_sample.csv
.duckdb
.duckdb-wal
*.duckdb
*.duckdb.wal

# Auto-add 2026-05-18: prevent large data files commit
data/*.duckdb
data/*.duckdb-wal
data/*.duckdb-shm
data/*.duckdb.backup_*
data/*.duckdb.bak_*
data/*.duckdb.tmp
data/*.duckdb.tmp/
data/portfolio_backtest_nav.csv
data/scratch/
data/reports/
data/lineage/research_prereg/
data/phase5_exports/

# sandbox/ — 隔离探索区 (用完直接删, 2026-06-17 用户根治: 探索散进主代码=污染根因)
# 探索脚本/草稿/结果/scratch DB 全在这, gitignored 进不了主代码; 唯一留存=experiment_store verdict
# 用 sandbox/* (非 sandbox/) 才能 negate README — git 不下降进被整体忽略的目录
sandbox/*
!sandbox/README.md

# Local agent/skill/plugin metadata. Project policy is tracked in AGENTS.md;
# executable skills live in $CODEX_HOME and are not duplicated in this repo.
.agents/
.claude/
.metadata_never_index
skills-lock.json

# BestChoice (moved from sibling 2026-05-22) — cache artifacts regenerable, exclude
bestchoice/__pycache__/
bestchoice/cache_*.duckdb
bestchoice/analysis/*.duckdb
bestchoice/.git/
.venv/
data/concept_snapshots/
data/archive/

# playwright CLI 本地控制台日志 (工具产物, 非项目资产; 2026-08-11 收口: 长期 untracked 让每次 moth takeover 假报脏 worktree)
.playwright-cli/

# 2026-08-31 止血: 这些是可重算的分析/实验产物, 单个 7~21MB, 已把 .git 撑到 3.9G
# (size-pack 2.90 GiB)。历史里的对象不清 (清史要重写 2615 个 commit + 11 分支 + force
# push, 代价远超省下的磁盘), 但从此不再随新 commit 增长。
# 例外: phase_e_experiment_verdicts/{manifest.json,b0.json} 仍版本化 —— manifest 被
# check_strategy_lab / agent_board_projection 读, b0 被 persist_phase_d 读且其 sha256
# 写进 manifest 参与证据链。
bestchoice/analysis/*.csv
data/lineage/phase_e_experiment_verdicts/b1.json
data/lineage/phase_e_experiment_verdicts/b2.json
data/lineage/phase_e_experiment_verdicts/b4.json
