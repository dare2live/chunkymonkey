# 东财全套迁移 Stage④ — 物删申万当前行业快照 dim (孤儿)
# owner=analysis/dc_full_migration_plan_20260623.md
# 前置: Stage② 已把全部 live 消费方切 dim_stock_dc_industry; fan-in 审计确认 dim_stock_sw_industry 零 live 读
#   (仅 build_sw builder[已剥]+1 audit list[已换dc])。深史 PIT 兜底 v_sw_industry_pit(tushare_raw)保留。
# 重建路径: tushare_raw.v_sw_industry_pit WHERE out_date IS NULL (1行1股当前归属), 故 action=drop 无需归档。
run_id: dc_migrate_stage4_sw_20260623
archive_dir: data/archive/lifecycle
entries:
  - table: dim_stock_sw_industry
    action: drop
    bucket: dead_after_migration
    reason: "东财全套迁移Stage④: 当前行业serving已切dim_stock_dc_industry; sw当前快照孤儿(0 live读); 深史PIT兜底v_sw_industry_pit保留; 可从v_sw_industry_pit WHERE out_date IS NULL重建"
