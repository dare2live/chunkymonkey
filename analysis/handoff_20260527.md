# Session Handoff — ChunkyMonkey 架构优化

> 日期: 2026-05-27
> 接力人: 下一个 Claude / Codex session
> 项目: `/Users/dp/Documents/M/stock/chunkymonkey`
> Branch: `main`
> HEAD: `2bda0976` (Revert "cleanup: 删 technical_specification + implementation_plan")
> 环境: `cd /Users/dp/Documents/M/stock/chunkymonkey && source .venv/bin/activate && export PYTHONPATH=backend`
> **开始前必读**: `goal.md` + `SESSION_HANDOFF.md` + `analysis/workflow_checkpoint.md` + 本文件

---

## 0. Codex resume validation update (2026-05-27)

| 项 | 结果 |
|---|---|
| Universe gate | `PYTHONPATH=backend python backend/scripts/check_universe_filter.py --all` -> CLEAN (767 files checked) |
| Universe governance | `services.universe` ST SQL now uses `universe_rules.yaml` `st_name_patterns`; `dim_active_a_stock` references in the universe service are annotated as ST name mapping / audit evidence, not active-universe truth. Scoped audit PASS with CodeGraph + per-file complexity + `test_universe.py` + checker tests. |
| Focused compile | `audit_end_to_end.py`, `build_architecture_inventory.py`, `seed_dim_data_asset.py` and touched tests -> PASS |
| Focused tests | current controller batch -> 54 passed (`test_build_price_kline_tdxhub.py`, `test_data_asset_governance.py`, `test_audit_tdx_data_need_coverage.py`); latest architecture-inventory slice -> 15 passed (`test_architecture_inventory.py`, `test_architecture_contracts.py`) |
| CodeGraph | `scripts/chunkyctl audit --run ...` -> executed `codegraph sync .`; `codegraph status .` still reports 49 Added pending. `scripts/chunkyctl doctor --fast` reconciles this as pending_added=49 matching untracked indexable `.py/.js/.jsx` files=49, so it is untracked worktree risk rather than a missing sync claim. |
| Tooling gate | Added local wrapper `backend/scripts/audit_tooling_gate.py`: git status JSON + CodeGraph status JSON + complexity markdown baseline/diff. Added `backend/scripts/chunkyctl.py` + `scripts/chunkyctl` + `docs/chunkyctl_session_quickstart.md` for doctor/worktree/preflight/audit. Removed the prompt printer; the durable new-session entrypoint is the quickstart doc. Quickstart is now the durable startup contract: any future startup-mode, gate, tool-entry, or controller/agent workflow change must update `docs/chunkyctl_session_quickstart.md` in the same batch and the handoff/final must say so. Smoke verdict is FAIL because current worktree has 183 dirty entries and CodeGraph pending 49. `scripts/chunkyctl doctor --fast` now includes worktree bucket summary and CodeGraph pending/untracked reconciliation; when no complexity baseline is loaded, diff status is `baseline_unavailable` and current HIGH are `unclassified`, not `new_high_count`; when `data/reports/tooling/complexity_baseline.json` exists, doctor loads it by default. 2026-05-31 generated the local ignored capped baseline and verified `doctor --fast` reports `baseline_status=loaded`, `diff_status=compared`, `new_high_count=0`, `unchanged_count=40`. `scripts/chunkyctl preflight` now token-matches task risks so `build` no longer trips the `ui` frontend gate. `scripts/chunkyctl worktree` classifies all 183 dirty entries with unknown=0 and now supports `--format markdown` for controller/agent review. `scripts/chunkyctl audit` now treats only `.py` files as Python compile/complexity scopes, so backend YAML/INI/SH config files no longer trigger false py_compile failures. |
| Startup/tooling P0 follow-up | Read-only agent returned REQUEST_CHANGES: quickstart missed `analysis/workflow_checkpoint.md`, and Rule 10 accepted empty `codex-review: skipped reason=`. Controller accepted the finding. Quickstart startup docs now include workflow checkpoint, latest `analysis/handoff_*.md`, bootstrap, and architecture context. `safe_commit.sh` now accepts only `Codex-Reviewed: APPROVE/APPROVE_WITH_NOTES` or an 8+ character skip reason; `Codex-Reviewed: REQUEST_CHANGES` and empty skip reasons are blocked. Added `backend/tests/scripts/test_safe_commit.py` and `safe_commit_rule10_tests` registry coverage using a temporary git repo dry-run. |
| Data-source/lineage P0 follow-up | 2026-05-31 local slice completed: `tdx_data_need_coverage.yaml` records 27 needs and now makes `grain`, `pit_key`, `freshness_sla`, `evidence_status`, and `production_eligibility` machine-required fields. `audit_tdx_data_need_coverage.py` rejects missing fields, invalid evidence/eligibility enums, and `eligible` needs with unknown PIT/freshness/grain; `schema_marts.py` DDL has the new columns. `need_027` 主力/超大/大/中/小单资金流 is explicitly `pit_key=unknown`, `evidence_status=unknown`, `production_eligibility=blocked`. Stale/deprecated `raw_fund_flow_daily` is no longer a production fallback in `CapitalFlowAlpha`, `build_institution_score_daily.py`, or `panel_pipeline_manifest.yaml`; raw remains research/deprecated until source probe + PIT/freshness gate. Verification: `scripts/chunkyctl audit --run ...` PASS, targeted pytest 25 passed, temp DuckDB materialization 27 needs / 10 priorities / 14 reassignments. Production DuckDB exact-sync not run. Profile/stock-dossier API/frontend remains blocked until profile component contract carries freshness, evidence status, and lineage. Quickstart unchanged. |
| Config project | `.gitignore`, `backend/config/field_dictionary.yaml`, and `pytest.ini` reviewed as `config_project` bucket. Scoped audit PASS together with `backend/config/test_tool_registry.yaml`, `backend/scripts/audit_test_tool_health.py`, `backend/tests/scripts/test_audit_test_tool_health.py`, and `docs/test_tool_governance.md`. `pytest.ini` excludes `realdb/perf/network/gcp/slow` by default; this is test-tool configuration evidence, not business-data or strategy evidence. |
| Test-tool audit | full audit PASS: 0 FAIL / 0 WARN / 365 selected / 365 registered / 0 unregistered / 100% coverage; root micro-batch 13a updater scoped audit PASS, pytest 89 passed / 2 warnings; root micro-batch 13b remaining root artifacts scoped audit PASS, default pytest 76 passed / 63 deselected / 15 warnings, realdb collect-only 63/66 collected; Rule 10 behavior tests registered |
| Test isolation fix | `test_run_feature_ablation.py` previously attached local `data/alpha158.duckdb` if present; loader now supports `with_alpha158=False` and the default test disables that realdb path |
| Complexity, scripts | `audit_end_to_end.py` is 0 finding; `build_architecture_inventory.py` latest slices removed `_safe_latest()` query-in-loop risk, fixed nested router prefix/route-match contract, changed JS comment stripping to a single-pass state machine, and made dependency context set-based; broad scans still report historical parser/scanner/dependency HIGH; `seed_dim_data_asset.py` improved to 6 HIGH / 8 MEDIUM; `build_price_kline_tdxhub.py` improved to 5 HIGH / 2 MEDIUM; `audit_tdx_data_need_coverage.py` is 0 HIGH / 1 MEDIUM |
| Complexity, backend | Full uncapped backend scan: 1,414 findings = 835 HIGH + 579 MEDIUM; default 80-line output is only `--max-findings` truncation, not total remaining debt |
| End-to-end audit | **FAIL**: report code runs, but data state has 3 freshness FAIL + 2 WARN; do not claim delivery health |
| Diff hygiene | `git diff --check` -> PASS |
| TDX data need governance | YAML/config contract is 27 coverage / 10 priority / 14 reassignment rows; script now exact-syncs obsolete rows, but this batch did not write production DuckDB |
| Parallel agents | 2026-05-27 controller spawned 3 read-only Codex explorers for complexity priority, parallel-agent workflow insertion, and end-to-end freshness/PIT root cause; controller owns all writes/merge |
| Controller mode | Codex default operating mode is now controller-led: controller owns direction, scope, final review, gates, shared docs, DB write windows, GCP/Optuna control, stage/commit; agents execute bounded scopes only |

Subagent findings absorbed:

| 主题 | 结论 | 下一步 |
|---|---|---|
| Complexity truth | Default complexity output is capped by `--max-findings=80`; latest uncapped backend scan is 1,414 findings = 835 HIGH + 579 MEDIUM. `audit_end_to_end.py` is fully clean by this scanner. | Do not say "80 remaining" as total debt. Prioritize remaining L0 K-line DB/orchestration risk, architecture inventory, data asset lineage, then feature governance. |
| Complexity next slice | 2026-05-31 read-only sidecar ranked next low-risk complexity targets: P0/P1 `build_architecture_inventory.py` remaining parser/scanner/dependency HIGH, then `build_drift_safe_feature_candidates.py`, then `backtest_model_portfolio.py`. It also found largest broad debt in `assets/js` and historical scripts, but those should not outrank architecture/PIT gates. | Next complexity work should pick one semantic hotspot with CodeGraph + focused tests, not chase raw HIGH count across UI/design/prototype files. |
| Updater boundary next slice | 2026-05-31 read-only sidecar found the main boundary risk is not `updater.py` line count but manager/domain leakage: `updater_execution.py` still knows K-line gap truth, and `updater_market_data.py` / profiles / trends / institution still hold domain write logic under router namespace. | Next updater work should move K-line connectivity/gap_queue blocked logic into market-data service/StepResult preflight, or build a runner registry to cut other routers off `routers.updater.RUNNERS`; do not add more domain logic to updater. |
| Top-level design audit | `docs/top_level_design_review.md` now states the architecture-management scheme itself is `APPROVE_WITH_NOTES`: reasonable under first principles and Occam, but not a completion certificate. | Controller must continuously optimize direction/targets by asking whether each slice reduces parallel ledgers, clarifies owner/truth source, and strengthens gates. |
| Accepted worker/controller slices | Accepted: `audit_end_to_end.py` full scanner clean; `build_architecture_inventory.py` report/indexing cleanup plus `_safe_latest()` batch latest-column scan, nested router prefix propagation, frontend route match index, JS comment stripper state machine, dependency context set maps; `seed_dim_data_asset.py` row-count N+1 + manual governance override + combined writer/reader scan; `build_price_kline_tdxhub.py` xdxr pure helper slice; `audit_tdx_data_need_coverage.py` exact-sync obsolete-row cleanup. Qualified: architecture inventory, seed and K-line files are not complexity-clean in uncapped backend scan. | Next controller slice should target one remaining semantic hotspot at a time, not broad rewrites. |
| Source coverage exact-sync | Ampere found governance table drift risk: YAML reassignments and materialized table can diverge if old rows are only upserted. | `audit_tdx_data_need_coverage.py` now deletes obsolete keys in transaction and has regression coverage; production DB refresh requires explicit DB-write decision. |
| End-to-end freshness | `fact_technical_trigger` / `fact_signal_context` stop at 2026-05-19; `mart_stock_survey_features` stops at 2026-05-20 while market K line / stock picture are newer. Likely missing updater/cron DAG production steps. | Design/patch DAG boundary first; DB writes must be serial and target-date explicit. |
| Recommendation PIT | Latest recommendation rows are all `cross_stage_fallback`; `n_same_stock_pit=3`, `n_same_stock_formula_pit=0`. | Do not use latest recommendation as PIT-safe production evidence until PIT parameter coverage or fallback rejection is fixed. |
| Parallel workflow | Long-term rules belong in `AGENTS.md` + `docs/agent_parallel_execution_policy.md`; current plan should link and summarize gate. | Controller-only writes to shared docs, DuckDB, GCP/Optuna, stage/commit. |
| Test-tool final root batch | Updater 9 root tests and remaining 14 root artifacts are now registered with owner/status/evidence. `system_routes` no longer imports production `main` at module import; `system_routes` and `v3_*` route smoke tests are `realdb` opt-in. | Treat full audit PASS as test-tool coverage only. Do not cite realdb collect-only as production data, route freshness, paper KPI, selection quality, or strategy evidence. |
| Deletion governance | Verified-dead code/tests/docs/config must be deleted directly after CodeGraph + `rg` + targeted test/audit evidence. Fake cleanup by comments, hidden flags, renamed files, dead branches, or empty stubs is blocked. | If deletion safety is not proven, keep the artifact active/quarantined with an explicit evidence gap; do not mask residue. |
| Tool update review | GitHub survey recorded in `docs/tooling_update_review_20260527.md`; decision is local wrapper first, external worktree/UI/history tools later. | Do not install new Git workflow tools while main dirty state is still unclassified. |

Latest added planning docs:

| 文件 | 内容 |
|---|---|
| `docs/profile_lineage_roadmap.md` | 股票画像、机构画像、主力行为画像、股票档案、架构后主线回归、最终 UI 重设计顺序 |
| `analysis/codex_app_update_checkpoint_20260527.md` | Codex Mac app 更新/重启后的安全接续点 |

---

## 1. 当前 Git 状态

**工作树仍 dirty，且包含本轮新增文档/updater/test 文件 (未 stage, 未 commit)**。下表是原 handoff 中的主线改动分类，不是当前完整 `git status`；接手时必须重新跑 `git status --short`，不要按旧的 20 文件假设 stage。

| 分类 | 文件 |
|------|------|
| **策略文件 (改 get_active_universe)** | `backend/services/screening_engine.py`, `backend/services/risk_factors.py`, `backend/services/audit.py`, `backend/scripts/build_picture_daily.py`, `backend/scripts/build_signal_context.py`, `backend/scripts/build_feature_panel_duck.py` |
| **Name-lookup 标注** | `backend/routers/institution.py`, `backend/routers/recommendation.py`, `backend/routers/v3_meta.py`, `backend/routers/v3_selection.py`, `backend/services/stock_detail_read.py`, `backend/services/stock_graph_read.py`, `backend/services/stock_trends_read.py`, `backend/services/external_attention.py`, `backend/services/recommendation_universe.py` |
| **Data-sync 标注** | `backend/scripts/build_price_kline_tdxhub.py`, `backend/scripts/ingest_holders_tdxhub.py` |
| **文档 (cron auto-update 产生的脏改动)** | `SESSION_HANDOFF.md`, `analysis/workflow_checkpoint.json`, `analysis/workflow_checkpoint.md` |

**Codex review 状态**: 17 个 backend 文件 diff 已 Codex review → APPROVE_WITH_NOTES (见 §4)。3 个文档文件无需 review。

**check_universe_filter.py --all 当前**:
- 默认 production-code only: CLEAN (767 files checked)
- `--include-tests`: 37 test fixture references (不阻断生产门禁)
- non-test: 0
- 起始值: 104 total (63 non-test)
- 已修: 63 non-test violations（含 5 处同一行 `rule-compliance` 标注、35 处 data-sync/schema/meta/name-lookup 标注）

**第一条 resume 命令**:
```bash
cd /Users/dp/Documents/M/stock/chunkymonkey
git diff --stat  # 确认当前 diff scope，不按旧 20 文件假设
PYTHONPATH=backend python backend/scripts/check_universe_filter.py --all  # 确认 CLEAN
```

---

## 2. 宪法 9 条执行状态 (逐条)

> 宪法文件: `docs/PROJECT_CONSTITUTION.md` (共 9 条)。
> CLAUDE.md §10-§12 是工程规则，不是宪法条款。

| 条 | 名称 | 状态 | 证据 | 缺口 |
|----|------|------|------|------|
| 1 | 真相源 | **生产门禁完成** | K 线 [OK]; 涨跌停 YAML [OK]; ST [OK]; 交易成本 [OK]; 公式参数 [OK]; universe lint CLEAN [OK] | test fixture 引用保留 `--include-tests` 人工审计 |
| 2 | 奥卡姆剃刀 | **部分完成** | dim_all_ever_listed 依赖已删 [OK]; non-test 冗余引用清零 [OK] | paper_sim 16 YAML 未合并 |
| 3 | 模块+数据表+配置 | **生产门禁完成** | 6 个 config/*.yaml [OK]; services 分层 [OK]; non-test `dim_active_a_stock` 有 evidence/API [OK] | 后续清理 test fixture 口径 |
| 4 | 分层架构 L0-L4 | **已定义, 无违规** | grep 确认 0 跨层 import [OK] | 无需建工具 (SKIP) |
| 5 | 验证前置 | **已完成** | 6 个 gate 全实现 [OK] | — |
| 6 | 审计工具体系 | **已完成** | 6.1-6.7 全实现 + 配置化 [OK] | 文档 6.1 写"硬编码"实际已配置化, 需同步 |
| 7 | 完成标准 | **已写** | — | 执行靠纪律 |
| 8 | 教训即规则 | **已写** | engineering-discipline skill 16 条 [OK] | — |
| 9 | 配置驱动 | **部分完成** | universe/audit/formula 全配置化 [OK] | paper_sim YAML 未合并 |

---

## 3. 下个 Session 任务清单 (按优先级)

### 3.1 [P0] 修 5 处标注位置

状态: 已完成并验证，`check_universe_filter.py --all` 从 81 降到 76，随后清完 non-test 并调整 checker 默认生产口径为 CLEAN。checker 规则仍保持: `rule-compliance: ok evidence=...` 必须在含 `dim_active_a_stock` 的**同一行**。

| 文件 | 行 | 问题 |
|------|-----|------|
| `recommendation.py` | :171 | `FROM dim_active_a_stock` 行无标注，标注在 :170 |
| `screening_engine.py` | :540 | SQL 字符串行无标注，标注在 :539 |
| `recommendation_universe.py` | :82 | `FROM dim_active_a_stock` 行无标注，标注在 :77 |
| `stock_detail_read.py` | :59 | `FROM dim_active_a_stock` 行无标注，标注在 :58 |
| `stock_graph_read.py` | :87 | `if _table_exists` 行无标注，标注在 :85 |

提交前不要按旧口径盲目 `git add backend/`；必须按本轮 scope 分批 stage，并通过 `scripts/safe_commit.sh` 的 Rule 10 gate。

### 3.2 [P0] 完成剩余 35 non-test 违规标注

状态: 已完成。`check_universe_filter.py --all` 默认 production-code only，CLEAN；`--include-tests` 仍报告 37 个测试夹具引用。

35 个违规分类及标注方式 (每条 `rule-compliance: ok evidence=<reason>` 加在 `dim_active_a_stock` **同一行**):

**DATA-SYNC** (枚举全量股票码做数据拉取 — dim_active_a_stock 是合法 master code list):
- `build_price_kline_tdxhub.py` (:168,:174,:179,:183,:189,:201,:208) — `data-sync-enumeration`
- `cron_daily.py` (:668) — `freshness-check-table-list`
- `ingest_holders_tdxhub.py` (:1221) — `lineage-metadata`
- `financial_client.py` (:1044,:1166,:1175) — `data-sync-enumeration`
- `financial_indicator_client.py` (:155) — `data-sync-enumeration`
- `capital_client.py` (:483) — `data-sync-enumeration`
- `aif10_capability_client.py` (:350,:363) — `data-sync-enumeration`
- `institution_write.py` (:110) — `data-sync-enumeration`
- `data_quality.py` (:3429) — `table-exists-check`

**NAME-LOOKUP** (code→name 映射):
- `run_daily_topk.py` (:490) — `code-to-name-mapping`

**SCHEMA/META** (表定义、lineage、审计配置):
- `schema_core.py` (:479) — `schema-definition`
- `schema_migrations.py` (:297) — `schema-definition`
- `security_master.py` (:6,:58,:65,:101,:104) — `table-writer-itself`
- `seed_dim_data_asset.py` (:107) — `metadata-registry`
- `data_lineage/registry.py` (:58,:81) — `lineage-metadata`
- `labels/universe.py` (:4) — `docstring-reference`
- `audit_stale_references.py` (:131) — `audit-metadata`
- `data_audit.py` (:120,:151,:370) — `audit-config-reference`
- `block_client.py` (:153) — `error-message-string`

### 3.3 [P0] Rule 10: safe_commit.sh 加 Codex review blocking gate

**问题**: `~/.claude/hooks/rule10_reminder.sh` 是 PostToolUse advisory hook, Claude 可以忽视。本 session 用户明确批评: "说明强制执行无效，你怎么能确保有效呢"。

状态: 已实现 Step 4.5 blocking gate；仍需随提交前校验一起跑 `bash -n scripts/safe_commit.sh`。

**方案**: `scripts/safe_commit.sh` 在 Step 4 和 Step 5 之间加新 step:
```bash
# Step 4.5: Codex review gate (Rule 10 blocking)
py_staged=$(git diff --cached --name-only -- '*.py' | wc -l | tr -d ' ')
if [[ "$py_staged" -gt 0 ]]; then
    has_codex=$(echo "$MSG" | grep -c "Codex-Reviewed:" || true)
    has_skip=$(echo "$MSG" | grep -c "codex-review: skipped reason=" || true)
    if [[ "$has_codex" == "0" && "$has_skip" == "0" ]]; then
        echo "ERROR: staged .py files 但 commit message 缺 'Codex-Reviewed:' 或 '# codex-review: skipped reason=<trivial/markdown/typo/rename>'"
        echo "修法: 先 codex:codex-rescue review diff, 拿到 verdict 再 commit"
        exit 6
    fi
fi
```

### 3.4 [P1] updater.py 拆分 (5136 行 → 17+ 模块)

状态: **第五十八刀已完成**。Codex/skill grill 后已抽无业务副作用的 runtime/helper、calendar/date-truth、DAG metadata 与 DAG 查询/计划过滤 helper、execution orchestration helper、full/group/smart/single 状态账本、group pipeline 执行循环 helper、full DAG 执行循环 helper、single-step chain 执行循环 helper、smart plan 执行循环 helper、background task failure/cleanup launcher helper、smart/full/single/group background launcher 依赖包、launcher callback bundle、group route request scheduling helper、run-start helper、step-status/status-plan 层，stale-running step_status 清理 helper，step_status catalog 同步 helper，source failure queue 状态 helper，update status payload/response helper，smart-update 计划/交易日历 preflight helper，audit snapshot refresh helper，audit route payload helper，connectivity probe/cache helper，K 线连通性预检 helper，reset table helper 与 reset route payload/connection lifecycle，standalone external sync/calc runner、sync_raw/sync_financial body，institution match runner、sync_industry body 与 build_industry_stat sync body，build_trends body，build_profiles body，sync_market_data body、lifeboat legacy endpoints，以及 data_completeness 覆盖率校准 helper。第三十八刀将 `/update/status` payload 组装迁入 `build_update_status_payload` 并新增 `backend/tests/test_updater_status.py`；第三十九刀将 audit snapshot refresh task/status helper 迁入 `backend/routers/updater_audit.py`；第四十刀将 `/update/audit` payload/连接生命周期迁入 `build_update_audit_payload` 并扩展 `backend/tests/test_updater_audit.py`；第四十一刀将 group pipeline 执行循环迁入 `run_group_steps`；第四十二刀将 full DAG 执行循环迁入 `run_all_steps`；第四十三刀将 single-step chain 执行循环迁入 `run_single_steps`；第四十四刀将 smart plan 执行循环迁入 `run_smart_steps`；第四十五刀将 full/smart/single/group 后台任务失败落账与 cleanup 迁入 `run_background_update_task` 并扩展 `backend/tests/test_updater_execution.py`；第四十六刀将 smart-update 计划/交易日历 preflight 组装迁入 `prepare_smart_update_plan` 并扩展 `backend/tests/test_updater_status.py`；第四十七刀将 smart background launcher 参数注入迁入 `UpdaterExecutionDeps` + `run_smart_update_background`；第四十八刀新增 `backend/routers/updater_launcher.py`，把 `UpdaterExecutionDeps`、`run_background_update_task`、`run_smart_update_background` 从 `updater_execution.py` 分离，并新增 `backend/tests/test_updater_launcher.py`；第四十九刀将 full/group/single launcher 参数注入迁入 `run_full_update_background` / `run_group_update_background` / `run_single_update_background` 并扩展 launcher tests；第五十刀将 reset-derived/reset-industry response payload/connection lifecycle 迁入 `build_reset_derived_payload` / `build_reset_industry_payload` 并扩展 `/update/reset-derived` 回归；第五十一刀将 `sync_industry` body/gap queue/progress JSON 迁入 `backend/routers/updater_institution.py::_step_sync_industry_with_hooks`，并新增 `backend/tests/test_updater_institution.py` wrapper 回归；第五十二刀将 `/update/status` 连接生命周期与 step_status catalog sync 迁入 `backend/routers/updater_status.py::build_update_status_response`，并扩展 `backend/tests/test_updater_status.py` catalog sync + close 回归；第五十三刀将 `/update/smart-plan` 连接生命周期与 plan budget response 迁入 `backend/routers/updater_status.py::build_smart_plan_response`，并扩展 `backend/tests/test_updater_status.py` critical-only budget + close 回归；第五十四刀将 `/update/reset-derived` 与 `/update/reset-industry-derived` 连接生命周期迁入 `backend/routers/updater_reset.py::build_reset_derived_response` / `build_reset_industry_response`，并扩展 `backend/tests/test_updater_reset_industry.py` close 回归；第五十五刀将启动前 step_status priming 连接生命周期迁入 `backend/routers/updater_steps.py::prime_run_step_status_for_steps`，并扩展 `backend/tests/test_updater_n_plus_one_fix.py` close 回归；第五十六刀将 `/update/smart` 计划构建连接生命周期迁入 `backend/routers/updater_status.py::build_smart_update_plan`，并扩展 `backend/tests/test_updater_status.py` close 回归；第五十七刀将 run context/noop/finish/heartbeat helper 迁入 `backend/routers/updater_status.py`，并扩展 `backend/tests/test_updater_status.py` 状态 helper 回归；第五十八刀将 group route request scheduling 迁入 `backend/routers/updater_launcher.py::launch_group_update_request`，并扩展 launcher busy/unknown/schedule 回归。`backend/routers/updater.py` 5136 -> 723 LOC；`backend/routers/updater_execution.py` 823 LOC；`backend/routers/updater_launcher.py` 278 LOC；`backend/routers/updater_reset.py` 161 LOC；`backend/routers/updater_steps.py` 232 LOC；`backend/routers/updater_status.py` 593 LOC；`backend/routers/updater_institution.py` 533 LOC。Complexity 仍显示其他遗留 HIGH，但无本轮新增 touched-file HIGH；`updater_market_data.py` 无 HIGH、仍有 MEDIUM membership-in-loop 债。

| 新模块 | 函数群 | 状态 |
|--------|--------|--------|
| `backend/routers/updater_infra.py` | _UILogHandler, _reset_ui_logs/get logs, metrics helpers, step detail/status normalize/format helper | **已抽出** |
| `backend/routers/updater_calendar.py` | calendar step, trading_calendar_status, calendar refresh, data-fetch preflight insertion | **已抽出** |
| `backend/routers/updater_steps.py` | _prime_step_status_rows, _sync_step_status_catalog_for_steps, _mark_steps_status, _mark_stale_running_steps_failed, _record_step_source_state_for_domains, _fail_unfinished_steps, _update_step, _resolve_step_result | **已抽出** |
| `backend/routers/updater_runtime.py` | _run_blocking_db_task, _run_blocking_market_db_task | **已抽出** |
| `backend/routers/updater_audit.py` | audit snapshot refresh task/status helper、sync refresh helper、`/update/audit` payload helper | **已抽出**；新增/扩展 `backend/tests/test_updater_audit.py` |
| `backend/routers/updater_status.py` | step budget/source domain、smart plan budget、critical daily filter、smart-update 计划/交易日历 preflight helper、status summary、update status payload/response connection lifecycle、smart-plan response connection lifecycle、downstream DAG helper | **已抽出**；新增/扩展 `backend/tests/test_updater_status.py` |
| `backend/routers/updater_reset.py` | reset table 常量、批量 table existence/count/delete helper、reset-derived/reset-industry response payload/connection lifecycle helper | **已抽出**；扩展 `backend/tests/test_updater_reset_industry.py` |
| `backend/routers/updater_connectivity.py` | connectivity probe、TTL cache、cached status helper | **已抽出** |
| `backend/routers/updater_sync.py` | sync_raw/LHB/QFII/AIF10/surveys/sync_financial external sync runner | **已抽出**；若 >1000 行拆 7+ 模块 |
| `backend/routers/updater_calc.py` | gen_events/calc_returns/current_relationship + financial/screening/sector/prediction/risk/external/stage/turtle/score/today-signal wrapper | **部分已抽出** |
| `backend/routers/updater_institution.py` | match_inst/exclusion helper、sync_industry body、build_industry_stat sync body | **已抽出**；后续评估 profiles 同域迁入 |
| `backend/routers/updater_trends.py` | build_trends body、K 线批量读取、趋势 helper | **已抽出**；`updater.py` 保留 thin wrapper 注入 stop hook |
| `backend/routers/updater_profiles.py` | build_profiles body、机构画像批量聚合、持仓周期 helper | **已抽出**；`updater.py` 保留 thin wrapper 注入 stop hook |
| `backend/routers/updater_market_data.py` | sync_market_data body、gap queue reconciliation、daily/monthly K 线同步、xdxr sync 编排 | **已抽出**；`updater.py` 保留 thin wrapper 注入 stop/update_step hook |
| `backend/routers/updater_lifeboat.py` | `/lifeboat/run/status/report` legacy endpoints、子进程运行、HTML 报告返回 | **已抽出**；`updater.py` include 子 router，API path 不变 |
| `backend/routers/updater_plan.py` | `STEPS`、`HARD_DEPS`、`SOFT_DEPS`、`MANUAL_ONLY_STEPS`、DAG 查询/计划过滤 helper | **已抽出**；新增 `backend/tests/test_updater_plan.py`，`routers.updater` 继续 re-export 兼容测试/调用 |
| `backend/routers/updater_execution.py` | hard-dependency blocking、remaining steps、K 线不可用 gap queue block/update fields、`StepRunProgress` full/group/smart/single 状态账本、`apply_step_result` step-result 落账/progress helper、`skip_if_kline_unavailable` K 线不可用 skip/gap_queue helper、`kline_connectivity_for_steps` K 线连通性预检 helper、`run_step_with_managed_connection` runner 独立连接生命周期 helper、`run_group_steps` group pipeline 执行循环 helper、`run_all_steps` full DAG 执行循环 helper、`run_single_steps` single-step chain 执行循环 helper、`run_smart_steps` smart plan 执行循环 helper | **已抽出**；新增/扩展 `backend/tests/test_updater_execution.py` |
| `backend/routers/updater_launcher.py` | `UpdaterExecutionDeps` launcher 依赖包、`run_background_update_task` 后台任务失败/cleanup helper、`run_smart_update_background` / `run_full_update_background` / `run_single_update_background` / `run_group_update_background` launcher helper、`launch_group_update_request` group route request helper | **已抽出**；新增/扩展 `backend/tests/test_updater_launcher.py` |
| `backend/routers/updater_completeness.py` | data_completeness 覆盖率校准、returns/industry coverage 检查、mart 表质量标记 | **已抽出**；新增 `backend/tests/test_updater_completeness.py` |
| `backend/routers/updater.py` | API routes 薄代理 | 待继续收薄 |

**不拆**: `data_quality.py` (4276 行, 0 HIGH) 和 `bc_absorbed/compute.py` (3303 行, 0 HIGH)。

**已跑验证**:
- `PYTHONPATH=backend python -m py_compile backend/routers/updater.py backend/routers/updater_institution.py backend/tests/test_updater_institution.py`
- `PYTHONPATH=backend python -m py_compile backend/routers/updater.py backend/routers/updater_status.py backend/tests/test_updater_status.py`
- lifeboat route smoke: `/api/inst/lifeboat/run`, `/api/inst/lifeboat/status`, `/api/inst/lifeboat/report` 3/3 OK
- industry/reset focused tests: 8 passed, 2 warnings
- nplusone/status/system focused tests: 42 passed, 2 warnings
- updater adjacent suite: 104 passed, 2 warnings
- post-cleanup smoke: 38 passed, 2 warnings
- market-data 前置相邻 targeted tests: 111 passed
- `PYTHONPATH=backend python backend/scripts/check_universe_filter.py --all` → CLEAN (767 files checked)
- `git diff --check` → PASS
- `check_project_index_sync.py` → PASS
- `bash -n scripts/safe_commit.sh` → PASS
- `codegraph sync .` → synced (27 changed files: 27 added, 445 nodes); `codegraph status .` 仍因新 untracked 文件提示 `Added: 27 files`
- 敏感扫描 (`dim_active_a_stock` / future-index / GCP / Optuna / paper_sim / backtest) → 仅命中 `updater.py` 两处历史注释
- 顶层设计审查已制度化 → 新增 `docs/top_level_design_review.md`，并同步 `docs/README.md`、`goal.md`、`docs/implementation_plan.md`、`SESSION_HANDOFF.md`。该文件把流程/功能/模块/数据表/配置文件的第一性原理、奥卡姆剃刀、owner、truth source、gate、失败模式变成架构变更审查模板，并补充“二阶审查”来审查这套管理方式本身是否合理。
- backend complexity scan → `v3_meta.py` P0 N+1 已清；`institution.py` P0 sort/nested-loop 已清；`screening.py` P0 nested-loop 已清；`v3_portfolio_builder.py` P1 nested-loop 已清；backend API HIGH 已清，scripts/audit/backfill HIGH 后续列 P1/P2
- Complexity P0-A: `backend/routers/v3_meta.py::get_formulas` 已由 per-formula 查询改为批量查询 map，新增 `backend/tests/test_v3_meta.py` batched source-table 回归；验证 `test_v3_meta.py` 27 passed, 2 warnings；`codegraph sync .` synced 29 changed files (27 added, 2 modified, 503 nodes)；backend complexity 不再报告 `v3_meta.py` HIGH。
- Complexity P0-B: `backend/routers/institution.py::get_institution_detail` 行业树由循环内排序改为 Counter 计数 + parent group 预排序 map，新增 `backend/tests/test_institution_read.py` helper 回归覆盖 Layer B 注释与排序；验证 `test_institution_read.py` + `test_institution_contract.py` 14 passed, 2 warnings；`codegraph sync .` synced 29 changed files (27 added, 2 modified, 529 nodes)；backend/routers complexity 不再报告 `institution.py` HIGH。
- Complexity P0-C: `backend/routers/screening.py::_to_name_keyed` 从 route 内嵌 nested loop 改为顶层 `_rename_sector_in_value()` + 浅拷贝转换，新增 `backend/tests/test_screening_read.py` helper 回归；验证 `test_screening_read.py` 3 passed；`codegraph sync .` synced 29 changed files (27 added, 2 modified, 477 nodes)；backend/routers complexity 不再报告 `screening.py` HIGH。随后已单独修复 `backend/tests/test_screening_engine.py` active-universe fixture 日期漂移，`test_screening_engine.py` + `test_screening_read.py` 4 passed。
- Complexity P1: `backend/routers/v3_portfolio_builder.py` regime 分段统计从 router 下沉到 `services.portfolio_walk_forward.regime.summarize_regime_segments()`，新增 `backend/tests/test_v3_portfolio_builder.py` endpoint 回归和 `backend/tests/portfolio_walk_forward/test_regime_cash_liquidity.py` service 回归；验证 20 passed；`codegraph sync .` synced 31 changed files (28 added, 3 modified, 500 nodes)；backend/routers complexity 不再报告 HIGH，backend 全量 complexity 剩余 HIGH 均在 scripts/audit/backfill 类工具。
- Complexity P1 gate script: `backend/scripts/audit_delivery_readiness.py` 已将循环内 `glob` 排序移到循环前预排序列表，并将 strategy model source mart 表存在性从循环内 information_schema 查询改为一次批量查询；`backend/tests/test_audit_delivery_readiness.py` 16 passed；单文件 complexity clean；backend 全量 complexity 不再报告该文件。未运行完整 `audit_delivery_readiness.py --json`，因为它会写 `data/reports/delivery_readiness.json`，本轮只做结构复杂度治理。
- Complexity P1 data gate script: `backend/scripts/audit_data_completeness.py` 已将每表循环内 connect/query 改为按 DB 分组后一次 UNION 汇总 max_date 与当日 n_codes；新增 `backend/tests/scripts/test_audit_data_completeness.py` 覆盖批量汇总和 verdict；验证 3 passed；`codegraph sync .` synced 30 changed files (30 added, 477 nodes)；backend complexity 不再报告该文件。脚本只读实跑 exit 1，真实数据风险为 6 张表 `STALE_7d⚠`。
- Complexity P1 scanner gate: `backend/scripts/audit_n_plus_one.py` 已将自身 root/file/body/iterrows 嵌套扫描拆成单层 helper，并把循环头 `sorted(...)` 改成预排序列表；不改变 finding 规则或报告格式。验证 `backend/tests/scripts/test_audit_n_plus_one.py` 15 passed；脚本实跑到 `/tmp` 为 30 findings / 22 HIGH / 8 LOW / baseline OK；`codegraph sync .` synced 30 changed files (29 added, 1 modified, 521 nodes)；backend complexity 不再报告该文件。
- Complexity P1 leakage gate: `backend/scripts/audit_panel_leakage.py` 已将 PIT marker schema introspection、fallback ratio、NULL year gradient、forward-index grep、universe PIT grep 和 summary print 拆成批量查询/单层 helper；check 6 从 per-feature SQL 改为一次按 year 聚合；新增 `backend/tests/scripts/test_audit_panel_leakage.py` 4 个 focused 回归。验证: `py_compile` pass；`test_audit_panel_leakage.py` 4 passed；与 `test_audit_n_plus_one.py` 合跑 19 passed；`codegraph sync .` synced 31 changed files (31 added, 529 nodes)；backend/scripts/backend complexity 不再报告 `audit_panel_leakage.py`。未跑真实大库 leakage audit，避免本轮写正式 report/长耗时。
- Complexity P1 PIT integrity gate: `backend/scripts/audit_pit_integrity.py` 已将 walk-forward batch/OOS 表规格扁平化，information_schema 表/列读取批量化，cross-date forward leak spot-check 改为扁平 specs + 单次 UNION ALL；新增 `backend/tests/scripts/test_audit_pit_integrity.py` 3 个 focused 回归。随后治理真实 PIT FAIL：`backend/scripts/import_bestchoice_phase1_candidates.py` 为 `mart_stock_formula_optuna_bestchoice_v1` 新增/迁移 `as_of_date` 与 `built_at`，默认 source 切到 repo 内 `chunkymonkey/bestchoice/analysis/...`，并重导入 1146 rows。验证: `py_compile` pass；`test_import_bestchoice_phase1_candidates.py` + `test_audit_pit_integrity.py` 5 passed；与 leakage/nplusone 相邻测试合跑 24 passed；`codegraph sync .` synced 33 changed files (32 added, 1 modified, 516 nodes)；backend complexity 不再报告 `audit_pit_integrity.py` 或 `import_bestchoice_phase1_candidates.py`。真实 `audit_pit_integrity.py` 实跑 PASS：9 PASS / 30 WARN / 0 FAIL。
- Complexity P1 event timestamp gate: `backend/scripts/audit_event_timestamp.py` 已复用 event table/column inventory helper，并将 timestamp non-null、PIT lag distribution、recent-30d sanity 改为 UNION ALL 批量指标；新增 `backend/tests/scripts/test_audit_event_timestamp.py` 覆盖 critical primary FAIL、missing-table WARN、future-date FAIL。验证: `py_compile` pass；`test_audit_event_timestamp.py` 3 passed；与 import/PIT/leakage/nplusone 相邻测试合跑 27 passed；`codegraph sync .` synced 34 changed files (33 added, 1 modified, 540 nodes)；backend complexity 不再报告 `audit_event_timestamp.py`。真实脚本实跑 PASS：55 PASS / 5 WARN / 0 FAIL；5 WARN 为历史异常负 lag 尾部，不能当 production 证据。
- Complexity P1 tradeability gate: `backend/scripts/audit_tradeability.py` 已将静态 grep 拆为 file-level helper，并将 spot check raw/view 逐日查询改为一次批量 UNION ALL 计数；新增 `backend/tests/scripts/test_audit_tradeability.py` 覆盖多 pattern 同行去重、raw/view 批量计数、PASS/FAIL/WARN 语义。验证: `py_compile` pass；`test_audit_tradeability.py` 3 passed；与 `test_audit_n_plus_one.py` 合跑 18 passed；`codegraph sync .` synced 35 changed files (34 added, 1 modified, 547 nodes)；backend complexity 不再报告 `audit_tradeability.py`。真实脚本实跑 PASS：4 PASS / 4 WARN / 0 FAIL；WARN 为本地 `price_kline` 无停牌样本、无近期停牌日、近 14 天无涨跌停 proxy、spot check 跳过，不能当完整生产覆盖证据。
- Complexity P1 survivorship gate: `backend/scripts/audit_survivorship_gate.py` 已将 ever-listed/active/panel counts 从多次查询合成一次 CTE，并将训练入口 `is_active=1` 扫描拆成 helper；新增 `backend/tests/scripts/test_audit_survivorship_gate.py` 覆盖批量计数、allow-list 保留、末行 match。验证: `py_compile` pass；`test_audit_survivorship_gate.py` 3 passed；与 `test_audit_n_plus_one.py` 合跑 18 passed；`codegraph sync .` synced 36 changed files (35 added, 1 modified, 551 nodes)；backend complexity 不再报告 `audit_survivorship_gate.py`。真实脚本实跑 FAIL：`mart_p0a_label_panel` 默认 label_version 仅 711 codes，低于 KEEP ever-listed 5,210 的 90% 门槛，不能当 survivorship-safe 证据。
- Complexity P1 universe coverage gate: `backend/scripts/audit_universe_coverage.py` 已将 business-table coverage 从 ref_date × table 逐次查询改为批量 K 线 universe 与业务表 codes map，并将 gap sample 排序抽到 helper；新增 `backend/tests/scripts/test_audit_universe_coverage.py` 覆盖批量 K 线、regular/alpha 日期列、panel/event 语义和稳定采样。验证: `py_compile` pass；`test_audit_universe_coverage.py` 4 passed；与 `test_audit_n_plus_one.py` 合跑 19 passed；`codegraph sync .` synced 37 changed files (36 added, 1 modified, 588 nodes)；backend complexity 不再报告 `audit_universe_coverage.py`。真实脚本实跑 PASS：16 PASS / 6 WARN / 0 FAIL；6 WARN 为 `fact_signal_context` 空行、近期 panel 空行和 partial-sync 样本，不能当 freshness 全绿证据。
- Hardcoding governance: `AGENTS.md`、`analysis/codex_bootstrap_20260527.md`、`docs/top_level_design_review.md`、`goal.md`、`docs/implementation_plan.md`、`chunkymonkey-governance`、`chunkymonkey-review-gate` 已纳入强制口径。业务规则/阈值/source catalog/数据源优先级必须先定 config/table/service owner；Python 只保留测试夹具、数学常量、schema/enum、SQL DDL 或有证据的例外。
- Complexity P1 TDX data need coverage gate: `backend/scripts/audit_tdx_data_need_coverage.py` 已清 fallback DDL query-in-loop，并将 TDX data need/source priority/reassignment catalog 从 Python 常量迁入 `backend/config/tdx_data_need_coverage.yaml`；默认 evidence path 改为实际存在的 `tdxhub/tdxhub/capabilities.py`；2026-05-27 补 exact-sync，事务内删除 YAML 已移除的 obsolete need/priority/reassignment key。验证: `py_compile` pass；`test_audit_tdx_data_need_coverage.py` 7 passed；默认配置为 27 coverage / 10 priority / 14 reassignment rows；`codegraph sync .` synced；backend uncapped complexity 为 0 HIGH / 1 MEDIUM；本轮未写生产 DuckDB exact-sync。
- Complexity P1 stale-reference gate: `backend/scripts/audit_stale_references.py` 已把 Tier 5/6/7 从 console-only 变成 JSON `summary` + arrays，commented-out-code 检测改为 AST/SQL 可解析口径，过滤公式说明、YAML 来源说明和英文说明性注释；新增 per-run `_read_lines` cache，降低 Tier1/Tier3/Phase0 重复全仓读取风险；Phase0 denylist 仍保持 report-only，未改变 blocking 策略。验证: `py_compile` pass；`backend/tests/scripts/test_audit_stale_references.py` 8 passed；真实 smoke `--no-fail --output /tmp/chunkymonkey_stale_audit_smoke.json` 当前 summary 为 critical_known_retired=0、warn_known_retired=0、tier4_parity_hits=0、tier5/6/7=0；单文件 complexity 为 0 HIGH / 2 MEDIUM，测试文件 0 findings；`codegraph sync .` synced。

**下一刀前**: New session 默认按 `docs/chunkyctl_session_quickstart.md` 接手；正式接手后跑 `scripts/chunkyctl doctor --fast`，dirty 时先用 `scripts/chunkyctl worktree --format markdown` 看总表，再用 `scripts/chunkyctl worktree --bucket <name> --format markdown` 下钻。quickstart 入口不绑定当前架构优化阶段，后续按最新用户请求、`goal.md` 和 handoff 继续；后续任何启动模式/gate/agent 工作法变化，必须同批更新 quickstart 并在交接里说明。当前 `doctor` 已内嵌 worktree summary 和 CodeGraph reconciliation: pending_added=49 matches untracked indexable files=49。当前 `worktree` 分桶: audit_gate_scripts 22, backend_services_api 29, config_project 4, controller_state 9, data_source_lineage_profiles 8, docs_archive_moves 6, pipeline_build_scripts 14, project_docs 11, startup_tooling 10, tests 36, universe_governance 4, updater_split 30, unknown 0。`docs_archive_moves` 已通过 controller + subagent 只读审计: 三份 root 文档与三份 `analysis/` 归档逐字一致，删除/归档后续必须成组 stage。清理顺序: P0 controller_state/startup_tooling/config_project/universe_governance/data_source_lineage_profiles/docs_archive_moves；P1 updater_split/audit_gate_scripts/backend_services_api/pipeline_build_scripts/tests；P2 project_docs 与历史 complexity backlog。继续 `/grill-with-docs`/architecture review，跑 `codegraph query "<target symbol>"` + `codegraph context "<target slice>"`；复杂度治理下一刀按 gate/feature-panel 影响排序，当前 backend complexity-optimizer 扫描仍有历史 HIGH，优先 `build_architecture_inventory.py`、`build_drift_safe_feature_candidates.py`、`backtest_model_portfolio.py`、`build_bestchoice_context_exit_policy.py` 等脚本；`backend/services/data_quality.py`、`backend/scripts/build_feature_panel_duck.py` 与本轮触达的 `backend/scripts/run_feature_ablation.py` 当前单文件扫描均为 0 finding，不再作为下一刀默认候选。测试工具可信度审查已进入 PRE-TEST GATE ACTIVE 且 full audit 当前 PASS：0 FAIL / 0 WARN / 365 selected / 365 registered / 100% coverage；第十轮完成 root micro-batch 13a/13b，updater 9 个 root tests targeted pytest 89 passed / 2 warnings，剩余 14 个 root artifacts 默认 pytest 76 passed / 63 deselected / 15 warnings，realdb collect-only 63/66 collected；Rule 10 行为测试已登记。跑任何测试前仍先跑/评审 test-tool audit；test-tool PASS 只证明 owner/scope/registry 对齐，不证明真实数据 freshness/PIT、route 生产可用、paper KPI、selection quality 或策略收益。PIT coverage 已 0 FAIL，但 30 WARN 仍不可用于 production claim；tradeability 真实脚本 0 FAIL 但 4 WARN 说明本地停牌样本覆盖不足；survivorship gate 真实脚本 1 FAIL 说明默认 label panel 覆盖不足；universe coverage 真实脚本 0 FAIL 但 6 WARN 说明部分 panel/fact_signal_context 仍需数据新鲜度审查。full/group/single launcher 参数注入已在 `updater_launcher.py`，后续不要把 route-level launcher helper 回塞 `updater_execution.py`；若继续拆 market data，先按 daily/monthly/xdxr 三段定接口，避免为本地大函数制造循环依赖。

### 3.5 [P2] paper_sim 7 死 YAML 清理

0 引用的 7 个文件直接 git rm:
```
paper_sim_cross_formula.yaml, paper_sim_ml_score_champion_baseline.yaml,
paper_sim_ml_score_champion_maxpos10_minhold15.yaml, paper_sim_ml_score_champion_minhold15.yaml,
paper_sim_ml_score_champion_minhold5.yaml, paper_sim_ml_score_sector_budget.yaml,
paper_sim_ml_score_tiered.yaml
```

### 3.6 [P1] 同步 docs/implementation_plan.md 状态

文档 vs 实际不一致, 必须同步:

| Task | 文档写的 | 实际状态 | 需改为 |
|------|----------|----------|--------|
| M2 dim 清理 | 待做 | **已完成** (get_active_universe 不依赖) | 已完成 |
| M3 data_audit 集成 | 待做 | **已完成** (daily_update Step 3c) | 已完成 |
| M6 data_audit 配置化 | 待做 | **已完成** (data_audit_rules.yaml) | 已完成 |
| S2 data_quality 拆分 | 待做 | **SKIP** (0 HIGH hotspot) | SKIP |
| S3 compute 拆分 | 待做 | **SKIP** (0 HIGH hotspot) | SKIP |
| "check_universe_filter lint CLEAN" | CLEAN | **CLEAN (767 production files); include-tests=37 fixtures** | 已完成 |
| M0 300616 公式 | 待做 | 待做 (用户说先架构) | 不变 |
| M5 GCP 跑批 | 待做 | **暂停** (用户要求先架构) | 暂停 |

### 3.7 [待交付] 架构全貌介绍

用户明确要求: "给我介绍一下架构全貌，改完是啥样的"。**未交付**。

应包含:
- L0-L4 分层图 + 每层模块清单
- 数据流: K 线 → universe → 公式 → 信号 → 策略 → 展示
- 6 个 gate 在流程中的位置
- config/*.yaml 控制什么
- 改前 vs 改后对比

---

## 4. Codex Review 记录

**对象**: dim_active_a_stock 治理 batch 1 (17 backend 文件 diff)
**Verdict**: APPROVE_WITH_NOTES

| 发现 | 严重度 | 行动 |
|------|--------|------|
| `get_active_universe()` 用 `CURRENT_DATE - 90 days` | MEDIUM | 实时 OK; 历史回测前需加 `as_of_date` 参数到 `universe.py` |
| Universe 略大于旧 dim_active_a_stock | LOW | 宪法正确行为; 验证下游消费者兼容 |
| `build_signal_context.py` 多余 import | LOW | **已修** (删除未用的 `get_active_universe`) |
| 6 策略文件 PIT leakage | CLEAN | — |
| Double-connection | CLEAN | market_conn 正确传递 |

**Codegraph**: sync up-to-date; callers 正确
**Complexity**: 6 策略文件 0 新 HIGH

---

## 5. 用户红线

1. **先架构后业务** — 不跳 M0/M4/M5, 架构做完才动业务
2. **跟 Codex 讨论好再执行** — 不边改边问用户
3. **Rule 10 必须 blocking** — advisory 已被证明无效; safe_commit 加 blocking gate
4. **每次改动后必跑 codegraph + complexity** — 每批改动后立即跑, 不攒到最后
5. **中文回复, 表格 > 段落, 数字优先**
6. **不报喜不报忧** — Gate FAIL / 数据滞后先讲

---

## 6. Suggested Skills

| Skill | 何时用 |
|-------|--------|
| `codex:codex-rescue` | **每次 .py 改动后必须 review** (Rule 10) |
| `/grill-with-docs` | updater.py 拆分执行前 grill 边界 |
| `/engineering-discipline` | 架构改动前走 6 步 checklist |
| `/diagnose` | 测试 regression 时走诊断循环 |
| `/handoff` | session 结束时更新交接 |

---

## 7. 验收标准

- [x] `check_universe_filter.py --all` non-test violations = 0
- [x] `safe_commit.sh` 含 Codex review blocking gate (Step 4.5)
- [ ] updater.py 拆成 8+ 模块并继续收薄 routes/helper-backed runners（infra + calendar + steps + sync + calc + runtime + institution 已完成，每个新模块 < 1000 行）
- [x] 本轮 `codegraph sync .` + complexity scan: 0 新 touched-file HIGH；`codegraph status .` 仍报 49 Added pending，需作为 untracked/worktree 风险继续披露
- [ ] 测试基线不回退 (1402 passed)
- [x] `docs/implementation_plan.md` 状态与实际一致（已记录 updater infra/calendar/steps/sync/calc/runtime）
- [ ] 宪法 9 条逐条有证据
- [ ] 三件套同步更新
- [ ] 用户得到架构全貌介绍
